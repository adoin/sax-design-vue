# Table 后续功能计划

状态：已按用户指令开始逐项实现。基线提交为 `6db6956`；完成项仅在对应实现、文档和验证通过后勾选。

已完成：第一阶段「拖动调整列宽」「列管理面板」「多级表头」「表尾数据行」「详情展开行」，第二阶段「单元格与整行编辑」「编辑生命周期」「数据校验」「变更追踪」「撤销与重做」，第三阶段「Grid 集成层」「查询表单与工具栏」「数据请求代理」「行拖拽排序」「基础键盘导航」「右键菜单」，第四阶段「跨行/跨列合并」「虚拟合并」「行分组与聚合」「单元格区域选择」「复制、剪切与粘贴」「查找与替换」「图表集成」。现进入公共约束与阶段整体验收；巨量运行时记录已完成，其余未勾选条目仍待逐项验证。

## 范围

- 以现有 `STable` 为基础，补齐已讨论的 VXE Table / Grid 能力，不另起一套表格渲染实现。
- 明确不做：表格导入、导出、打印，以及对应的工具栏入口。本排除项不要求删除项目已有的独立组件。
- 区域选择、复制粘贴、查找替换、聚合和图表也纳入后续计划；它们属于较后的阶段，不因 VXE 的版本划分而遗漏。
- 以下模块名称和配置设计仅用于规划，正式 API 在对应阶段实现前确定。

## 当前基础（保留，不重复建设）

已有 `data + columns` / `STableColumn`、内置分页、跨页选择、单列与多列排序、列级比较器、本地与远程筛选、树形与懒加载、固定列、双向虚拟滚动、动态行高、渲染器和溢出提示。

保留数据驱动架构，不恢复 `STr` / `STd` / `STh`；保留消费者行数据的所有权，不为索引向原始数据添加内部字段。现有压力示例不等于所有功能组合都已通过容量认证。

## 第一阶段：通用展示与列管理

- [x] **拖动调整列宽**：支持拖动、最小宽度限制和宽度变更事件；维护固定列偏移、横向虚拟列窗口与动态行高缓存的正确性。
- [x] **列管理面板**：支持列显隐、列顺序调整、左右固定设置、恢复默认；支持可选的配置持久化及外部受控状态。
- [x] **多级表头**：配置式和声明式列共享分组表头模型；支持嵌套分组、跨列标题及叶子列排序/筛选，处理显隐、固定列和横向虚拟滚动。
- [x] **表尾数据行**：支持一行或多行表尾、格式化与自定义渲染；表尾与数据列对齐，共享宽度、固定列及横向滚动，不以普通 `footer` 插槽代替。
- [x] **详情展开行**：独立于树节点展开，可放详情、表单或子表格；支持受控展开、键盘操作、异步内容和动态高度测量，兼容固定列及虚拟滚动。

## 第二阶段：编辑、校验与变更管理

- [x] **单元格与整行编辑**：配置编辑器、编辑触发方式、可编辑条件、进入/提交/取消编辑事件；复用项目已有输入、选择和日期等控件。
- [x] **编辑生命周期**：管理活动编辑单元格、焦点与草稿；明确翻页、排序、筛选、滚动卸载及外部数据更新时的提交/取消策略。
- [x] **数据校验**：必填、类型、自定义同步/异步规则，单元格/行/全表校验，错误展示与滚动定位，防止过期异步校验覆盖新结果。
- [x] **变更追踪**：记录新增、删除和修改的数据，提供查询变更、插入、删除、还原单行/全部的能力；区分编辑草稿和业务已确认数据。
- [x] **撤销与重做**：在稳定行键基础上定义编辑、增删操作历史及清空时机，避免对巨量数据无条件复制完整快照。

## 第三阶段：Grid 业务集成与表格交互

- [x] **Grid 集成层**：组合查询表单、工具栏、现有分页和 `STable`；只负责业务编排，复用同一个表格渲染、排序、筛选和选择管线。
- [x] **数据请求代理**：统一查询、刷新、保存、删除与分页/排序/筛选参数；处理 loading、异常、请求竞态及可取消请求，保留手动管理数据的方式。
- [x] **查询表单与工具栏**：支持配置和插槽、查询/重置、刷新、列设置及业务操作按钮，不添加导入、导出、打印入口。
- [x] **行拖拽排序**：支持拖拽手柄、禁用条件、变更事件与受控数据更新；明确排序、筛选、分页及树形下的行为，并处理虚拟窗口边缘自动滚动。
- [x] **基础键盘导航**：活动单元格与选中行分离；支持方向键、Tab、Enter、Escape、焦点恢复及跨虚拟窗口定位，不干扰编辑器和中文输入法。
- [x] **右键菜单**：支持表头、数据单元格和表尾上下文，动态菜单与禁用条件，键盘唤起与关闭；复用共享弹层和菜单组件。

## 第四阶段：复杂数据布局与 Excel 式操作

- [x] **跨行/跨列合并**：支持声明式合并及计算规则，覆盖数据区和表尾；明确合并区域与排序、筛选、分页的关系。
- [x] **虚拟合并**：当合并起点在视口之外时仍正确显示；解决跨固定列边界、横向/纵向窗口切换、动态行高及命中定位，不能仅支持普通小表格。
- [x] **行分组与聚合**：支持按字段分组、展开/收起、分组小计及整体汇总；支持内置与自定义聚合函数，明确统计范围和远程计算接口。
- [x] **单元格区域选择**：支持鼠标及键盘选择区域、边缘自动滚动；统一固定列和虚拟列的逻辑坐标，处理合并单元格。
- [x] **复制、剪切与粘贴**：支持区域文本/二维数据、只读单元格、编辑校验及批量变更事件，明确取消与撤销策略；剪贴板操作不等于文件导入/导出。
- [x] **查找与替换**：支持匹配导航、单项/批量替换、搜索范围及只读限制，并与变更追踪、校验、虚拟定位衔接。
- [x] **图表集成**：从选中区域、筛选结果或聚合结果构造图表数据；采用可选适配层，避免为普通表格强制引入图表依赖。

## 模块边界与公共约束

- [x] 列布局、分组表头、表尾、详情展开分别组织；编辑、校验、变更历史、区域选择和请求代理采用职责单一的 composable，不继续堆入 `table.vue`。见 [模块与列管线验收](reports/table-architecture-audit.md)。
- [x] 配置式列与声明式列进入同一套模型和渲染管线，保留既有单元格渲染优先级。已修复声明式重排、动态列插槽和表格级插槽失效，8 项专项与 602 项相关回归通过；双语 Code/Playground 实测通过。
- [x] 默认行为不变，新能力显式开启；新增受控状态、事件和暴露方法同步补全 TypeScript 类型。见 [默认行为与类型验收](reports/table-opt-in-audit.md)：7 项开关专项、609 项相关回归、7 项文档测试及五项类型检查通过，已修复虚拟状态类与实际布局开关不一致。
- [x] 新增弹层统一复用 `SPopper`；辅助交互复用仓库现有组件，遵循当前主色、阴影、形状、暗色和禁用态风格。
  - 见 [弹层与形状验收](reports/table-overlay-audit.md) 和 [筛选与 Grid 验收](reports/table-grid-ui-audit.md)：修复焦点争夺、形状漏传、筛选越界及加载/禁用态遗漏。两组隔离浏览器均覆盖八种屏宽/主题/形状组合，628 项相关回归通过；模态图表复用共享 `SDialog`。
- [ ] 虚拟渲染仅处理可视窗口与预渲染区；固定列与主体使用一致的行高和坐标，避免错行、内容透出及横向滚动时高度回缩。
  - [固定列与虚拟布局检查](reports/table-layout-audit.md) 已修复分组小计重复横向位移和动态测量后焦点停在视口外；32 种组合、136 个状态通过，另覆盖用户输入中断和卸载。所有新功能的完整组合验收仍保持未完成。
- [x] 列宽或内容改变时明确测量缓存的失效规则；区分同一布局下保留行最大测量高度与真实布局变化后的重新测量。见 [测量缓存验收](reports/table-measurement-audit.md)：普通数组与百万行生成源共 34 个浏览器阶段、53 项相关测试通过；双语文档明确引用替换、原地更新与 `measure()` 的区别。
- [x] 巨量数据操作明确本地/远程处理范围、内存成本和取消机制，不把浏览器 Map 容量当成表格可用行数保证。见 [操作范围与成本验收](reports/table-operation-scope-audit.md)：核对查询、分组、校验、查找、剪贴板、图表、历史/变更、拖拽、详情和测量缓存，292 项专项与 7 项文档测试通过；双语说明同步区分同步计算、异步取消和实际内存成本。

## 各阶段验收清单

- [ ] 单元测试覆盖默认行为、边界、受控更新、异常、卸载清理及键盘操作。
- [ ] 组合测试覆盖普通数据、分页/跨页选择、树形/懒加载、固定列、双向虚拟滚动、动态行高；新能力不能只在孤立示例中有效。
- [x] 巨量场景记录初始化、连续滚动、跳转、窗口渲染数量、缓存和内存表现；主动注明尚不支持的功能组合。见 [运行时验收记录](reports/table-runtime-audit.md) 和同目录前后测量 JSON。
- [ ] 同步中英文示例文案、渲染示例、完整 Code 源码与 Playground；例子说明和控制均放在同一个 `<card>` 内。
- [ ] 更新逐项 API 文档与项目类型详情，运行 `pnpm run normalize:doc-examples` 并审核结果。
  - 公开 API 名称、静态默认值和六页实际渲染核对见 [API 文档核对](reports/table-api-audit.md)；已补齐遗漏并修复重复锚点和失效用法链接。[动态插槽验收](reports/table-slot-audit.md) 已修复迟到插槽及虚拟行内容不更新，并核对作用域参数。[方法参数核对](reports/table-method-contract-audit.md) 已补充 82 个方法的双语签名检查，修复原始行对象定位/校验和 measure 返回类型；嵌套 options 的全部语义及 TableSelect 的独立签名核对仍待完成。
- [x] 运行相关组件测试、`pnpm run test:docs-examples` 和类型检查；共享文档管线变更另运行 `pnpm run docs:build`。见 [类型与测试验收](reports/table-validation-audit.md)：720 项相关回归、5 项文档测试和完整五项类型检查通过，本阶段未变更共享文档管线。
- [ ] 浏览器检查中英文 Code/Playground、明暗主题、键盘焦点，以及每项新功能与固定列/虚拟滚动的组合。

## 列宽调整验证记录（2026-09-04）

- `resize-config` 默认关闭；配置列与声明式列共用实现。支持受控 `column-widths`、取消拖动、最小宽度、键盘调整和卸载清理，消费者数据及列配置保持不变。
- 拖动提示默认隐藏，鼠标悬浮、键盘聚焦或拖动时出现。浏览器测量确认热区中心与真实边界重合；右固定列调整 120 → 160px 后表头、数据列位置一致。
- 中文示例说明列从 240 → 410px 后，首行由 150px 降到 85px；横向窗口切换仍保留当前布局下已测得的最大行高。
- 10 万行 × 10 万列按需生成示例只渲染约 8–14 行、9 个表头；已检查末行 99999、末中心列 99998、固定列和调整后定位。单测另覆盖 16,777,216 个逻辑列，只为改过的列保存宽度。
- 中英文示例、完整 Code 与 Playground 均实测；检查浅色、暗色和键盘调整。相关组件测试 64 项通过，文档示例/API 测试 4 项通过，组件和文档类型检查通过。
- `typecheck:vitest` 仍有旧测试类型错误（Card、Carousel、VirtualList、utils 等）；本次新增列宽测试未出现类型错误。阶段级性能/内存基准与完整组合验收尚未完成，不因上述窗口计数而勾选公共验收项。
- 首次验收发现既有纵向虚拟列表受浏览器最大布局高度限制：100 万行、估算 64px 的总高度超限，无法正确到达末端。该问题已在下述补充验证中修复。

### 纵向巨量定位补充验证（2026-09-04）

- 生成数据超过安全布局高度时，使用最长 800 万像素的原生轨道，并把可见行映射到实际视口；逻辑行高、行索引、`scrollToOffset` 的逻辑像素契约不变。只渲染窗口，未复制完整数据矩阵。
- 恢复列宽示例为 100 万行 × 10 万列。浏览器已确认末行 999999 和末中心列 99998；末端只挂载 8 行、9 个表头。原生滑块拖到中段后落在约 49 万行，挂载 18 行；轨道始终保持 800 万像素。
- 从末端滚轮上移 80px，同一行的屏幕坐标移动恰好 80px；不会按压缩比例放大滚轮步长。左右固定列保持对齐。
- 末端把中心列 120 → 170px 后重新测量，最后一行仍与滚动视口底部重合。拖动期间不补偿 `scrollTop` 的保护继续通过回归测试。
- 中英文完整 Code 与 Playground 已确认使用百万行源，两个 Playground 均能到达末行 999999。相关表格/虚拟列表测试合计 69 项通过（包含卸载后停止补偿的专项检查），文档/API 测试 4 项、组件类型检查通过。
- 本次验证针对桌面浏览器的生成数据、滚轮、原生滑块和编程定位，不等同于阶段级完整性能、内存、触屏手势及所有功能组合认证；公共验收项仍保持未完成。

## 行选择与文档修订（2026-09-04）

- 行选择统一使用 `v-model:row`，单选值为行对象或 null，多选为数组；同步 TableSelect 内部调用、中英文示例和 API。旧绑定保留迁移兼容，显式 `row` 优先。
- 选中行保留原背景，以贴近行边缘的阴影突出状态；检查浅色、暗色、单选、多选，以及 Code/Playground 双语交互。相关 Table、TableSelect、VirtualList 测试 79 项、文档测试 4 项通过，组件和文档类型检查通过。
- 对外表格文档改为 API 与使用说明，移除实现指令、内部容量讨论和验证口吻。示例可见文案与完整源码同步；后续文档继续面向组件使用者编写。

## 参考

- [VXE Table 官方功能清单](https://github.com/x-extends/vxe-table#功能点)
- [VXE Table 官方文档](https://vxetable.cn/)

此清单描述目标与依赖顺序，不代表已完成，也不承诺一次性交付全部能力。

## 列管理与行高亮验证（2026-09-04）

- `column-manager-config` 默认关闭；`column-state` 控制显隐、完整列序与固定位置，配置列和声明式列共享布局。列管理使用独立的排列模块、状态 composable 和面板组件，未修改消费者列与行对象。
- 面板复用 SPopper、SCheckbox、SSelect、SButton、SVirtualList 和 SFocusTrap。检查了键盘重排、PageUp/PageDown、Escape 焦点恢复、嵌套固定位置选择，以及 Playground 中先关闭面板再关闭主弹窗；浅色、暗色、加载禁用均实测。
- 可选 `storageKey` 只在显式配置时读写 localStorage；非受控状态挂载恢复，受控状态不覆盖父级值。浏览器确认刷新后恢复隐藏列，重置保存空设置；单测覆盖存储失败和切换键，失败不阻断表格操作。
- 调整过的列以稀疏状态记录，面板按需读取窗口。100 万行 × 10 万列下隐藏、重排后仅挂载约 9 个设置项、9 个表头、11–13 行；确认末行 999999、末中心列 99998 和末端列宽调整。单测另覆盖 16,777,216 列的重排、隐藏、固定、列宽和隐藏选择列后的模型保持。
- 普通数据的隐藏列保留排序、筛选和选择语义；重排与固定设置后仍按稳定键调整列宽。组合测试覆盖声明式列、树节点、分页、动态行高和双轴窗口。上述计数不代表完整阶段的内存或性能认证，公共验收项继续保留。
- 根据最新反馈，行选择公开模型改为 `v-model:highlight`，优先级为 highlight、兼容 row、兼容 modelValue。亮色采用主色 HSL 偏转到暖色范围后的浅杏色背景；暗色降低混合强度，保留弱阴影、零边框。单选、复选、旧模型兼容及 TableSelect 内部调用已验证。
- 新列管理、持久化及更新的行选择示例，均同步验证中英文渲染、完整 Code 与 Playground。相关 Table、TableSelect、VirtualList 测试 89 项通过；文档示例/API 测试 4 项、组件与文档类型检查通过。源码 include 规范化仅修改对应示例行号。


## 多级表头验证记录（2026-09-04）

- 配置式 `children` 与声明式 `STableColumn #columns` 共享列树及叶子模型。`#default` 保留单元格作用域，`#header` 可定制组标题；空组不生成数据列。固定位置向下继承，显式 false 可解除继承，不修改消费者列对象。
- 分组标题按实际连续叶子计算跨度，较浅叶子跨行显示。显隐、重排、固定分区和虚拟窗口会正确拆分标题段；叶子继续复用排序、筛选、选择、调整列宽和溢出提示。更新表头行数和数据行的 ARIA 索引。
- 分组树、窗口投影和表头行布局分别位于独立模块。未分组时沿用单行 flex 表头；分组布局按相同固定宽度、最小宽度和剩余空间分配规则对齐。浏览器普通与横向虚拟滚动实测坐标一致，弹性列仅存在不超过 0.02px 的布局舍入差异。
- `virtualSource.headerPath(index)` 只读取可见及预渲染列和固定列，`headerDepth` 固定表头行数。100 万行、10 万列的页面与中英文 Playground 都到达末行 999999、末中心列 99998；当前窗口为 10 行、13–14 个表头单元格。该记录不代表完整内存、性能或所有功能组合认证。
- 新增 6 项测试覆盖嵌套跨度、查询、显隐重排固定、声明式更新与卸载、继承、不变数据、懒树/分页/选择、巨量窗口。Table、TableSelect、VirtualList 及文档测试共 99 项通过；后续统一组标题内容渲染后，相关分组/查询测试另 28 项通过。组件和文档类型检查通过。
- 三个示例的中英文渲染、完整 Code 和 Playground 全部检查；验证虚拟开关、键盘列宽调整、隐藏叶子后跨度收缩、末端定位，以及浅色/暗色 HSL 背景、阴影和零边框。源码 include 已规范化，公共阶段验收清单继续保留未完成。


## 表尾数据行验证记录（2026-09-04）

- `footerData` 提供独立记录，支持单行、多行和稳定 `footerRowKey`。复用当前叶子列、列宽、固定偏移与横向窗口；保留表格底部 `footer` 工具栏插槽，表尾自身不参与正文查询、分页或选择。
- 支持指定列/通用表尾插槽、声明式列 `#footer`、列 footer 函数、命名/内联渲染器 footer、footerFormatter 与原始字段回退；按此顺序解析，正文渲染器不会套到表尾。提供独立 footerAlign、showFooterOverflow 和 footerCellClick。
- 表尾行由独立组件渲染；独立高度 composable 在同一布局的横向窗口间保留最大行高，列布局、宽度、数据或显式 measure 会重测。验证观察器清理、数据更新、稳定行键、空正文、格式化优先级和事件隔离。
- 浏览器发现既有 data-view 的 overflow:hidden 会成为普通固定列的参照滚动容器，右固定列被裁切；改成 overflow:clip，保留圆角裁切并让固定列跟随实际滚动容器。表头、正文和表尾在普通/虚拟横向滚动下坐标一致，键盘将金额列从 160 调至 170px 后三层宽度一致。
- 空正文时使用外层原生横向轨道，并让数据表面提供实际物理宽度；有虚拟正文时仍由正文滚动容器驱动，避免双重平移。切换空正文未增加既有巨量源的初始化行读取次数。
- 100 万行、10 万列示例采用显式汇总公式，不遍历生成行列；页面和双语 Playground 均验证末行 999999、末中心列 99998，以及空正文的末列定位。浏览器当前窗口约 12 个正文行、两行表尾共 10–12 个单元格，空正文轨道宽 100320px。此记录不是全面内存/性能或所有组合认证。
- 三类示例的中英文渲染、完整 Code 和 Playground 全部验证；检查单行/多行切换、传入数据范围变化、声明式与底部插槽并存、明暗主题、弱阴影和零边框。相关 Table、TableSelect、VirtualList 及文档测试共 105 项通过，组件与文档类型检查通过；源码 include 已规范化。阶段级公共验收继续保留未完成。

## 详情展开行验证记录（2026-09-04）

- 配置式与声明式 `type: 'expand'` 列使用同一详情入口。`detailConfig`、`detailExpandedKeys` 与树展开、行高亮分别管理；支持初始展开、条件限制、受控接受/拒绝、公开展开/收起与重新加载方法，不修改业务数据。
- 状态与请求放在独立 composable，数据行与详情由同一个行块承载。`#detail`、`#detail-loading`、`#detail-error` 提供独立内容入口；默认错误界面复用 SButton 重试。加载去重、收起清理、AbortSignal、过期响应忽略、数据数组替换及卸载清理均有测试。
- 详情开关使用原生按钮，验证 Tab、Enter、空格和收起后的焦点恢复。详情区域 ID 在同键的多个表格间保持唯一；普通数据的 ARIA 行索引计入详情与表尾，生成源不会遍历全量数据计算详情索引。
- 虚拟详情强制动态测量，横向窗口间保留当前布局下最大行高；详情缩短或关闭时重新测量。未开启详情不添加行包装 DOM，未展开面板不创建尺寸观察器。浏览器普通虚拟示例移除子表格后，详情高度从约 317px 收缩到 169px，行块同步从 369px 收缩到 221px。
- 100 万行、10 万列的中英文页面/Playground 验证末行 999999、末中心列 99998、左右固定列及可见区域宽度的详情面板；窗口只挂载约 7–10 行、7–9 个表头。末端详情缩短后底部误差小于 1px，收起后底部精确对齐且焦点返回该行按钮。此项不是完整性能或内存认证，公共阶段验收仍未完成。
- 表单与子表格、异步加载、巨量源三类示例的中英文渲染、完整 Code 和 Playground 全部验证；检查暗色窄预览、零边框、弱阴影、错误重试和业务表单值保留。相关 Table、TableSelect、VirtualList 及文档测试共 117 项通过，组件与文档类型检查通过；源码 include 已规范化。

## 单元格与整行编辑验证记录（2026-09-04）

- `editConfig` 显式开启单元格或整行编辑，支持双击、单击、手动触发、F2 和可编辑条件。默认编辑器复用 SInput、SSelect、SDatePicker、SSwitch；数字输入转换为数值。配置式插槽、声明式 `#edit` 和具名编辑渲染器共用管线，展示态与编辑态分别解析。
- 独立编辑 composable 管理活动会话、字段草稿、提交与取消。仅复制变更路径，不修改消费者行数据；`editCommit` 提供 changes 与 updatedRow，由业务接收。验证外部同字段冲突、快照隔离、父组件接受提交后的下一单元格切换，以及卸载后的回调清理。
- Enter 提交、Escape 取消，中文输入法合成中不提交；Ctrl/Meta+Enter 支持选择器等控件。日期和选择弹层先处理自身键盘交互；浏览器验证日期第一次 Escape 只关面板，第二次才取消编辑。编辑结束恢复单元格焦点，虚拟卸载保留草稿后再次定位会恢复输入焦点。
- 百万行、十万列示例仅记录已保存的单元格；双语页面/Playground 均到达末行 999999、末中心列 99998。浏览器窗口约 11–13 行、6–8 个表头，整行编辑单测少于 15 个控件；末端提交后重新进入仍读取已保存值。此记录不等同于完整性能或内存认证。
- 三类新示例的中英文渲染、完整 Code 与 Playground 全部检查，包括内置控件、自定义编辑器和生成源；检查明暗主题、窄预览、零边框及弱阴影。Table、TableSelect、VirtualList 与文档测试合计 131 项通过，组件和文档类型检查通过；源码 include 规范化无额外变更。
- 当前已提供切换、查询/分页、滚动卸载及数据替换的基础策略。独立「编辑生命周期」条目保留未完成，下一步补齐树形/懒加载与外部更新等组合审核；校验、变更追踪和历史操作尚未实现。

## 编辑生命周期验证记录（2026-09-04）

- 独立生命周期 composable 统一处理查询、分页、配置式/声明式列变化和当前行有效性。默认切换单元格提交、查询/分页/列变更取消，均支持显式策略；受控查询拒绝更新时保留草稿，接受更新后才执行策略。原地更新筛选数组也会触发检查。
- 同一数组内替换同键行对象时以 data 原因取消；删除行、收起祖先节点或生成源缩小使活动索引越界时以 view 原因取消，不会提交失效记录。复用既有稳定键索引，不遍历生成源；兄弟节点懒加载导致行位移时保留草稿并更新事件行索引。
- 三种 onScroll 策略均通过实际虚拟编辑器卸载测试。默认 keep 在返回窗口后仍提供草稿；显式 startEdit 恢复焦点。commit/cancel 在卸载后分别发出 scroll 原因，普通分页和查询变化优先使用 page/query 原因。
- 新增双语生命周期示例可切换策略、分页、虚拟滚动、模拟同数组行替换及重新加载。中英文完整 Code 和 Playground 均验证；中文虚拟窗口为 11–12 行，编辑器移出后卸载且保存按钮仍可用，返回后草稿/焦点恢复并正常提交。
- 英文页面确认分页提交，Playground 确认默认分页取消；暗色预览中收起祖先后取消子行编辑，焦点保留在展开按钮。编辑表面使用现有 HSL 弱阴影、零边框，实际窗口下策略控件和操作按钮正常排列。
- Table、TableSelect、VirtualList 与文档回归共 139 项通过，组件和文档类型检查通过；新增 8 项生命周期组合测试涵盖行替换/删除、懒树、受控筛选、分页、声明式列、虚拟卸载及生成源边界。源码 include 已规范化；数据校验、变更历史及阶段级完整容量/性能认证继续保留未完成。

## 数据校验实现进度（2026-09-04，未完成）

- 已新增独立规则模块与可取消的批量状态模块，支持必填、类型、范围/长度、正则及自定义同步/异步规则。规则不修改输入值或消费者 RegExp 状态；错误快照与内部状态分离。
- 批量入口接收惰性目标迭代器，每 100 个目标让出执行时间，支持 AbortSignal、错误数上限及明确的 cancelled/truncated 结果。取消能立即结束等待，即使自定义异步校验没有响应 signal；过期结果不覆盖新的错误状态。
- 编辑状态已提供内部提交校验钩子，保留无校验时的同步路径。校验失败保留草稿；同一草稿的并发提交复用请求；继续输入、取消或外部字段冲突后旧结果不能提交，切换单元格会等待异步校验结果。
- 规则/校验会话新增 11 项测试，提交钩子新增 4 项测试；编辑、生命周期与校验专项合计 37 项通过。当前仍是内部基础，尚未连接 STable 公开校验配置与方法、错误展示、跨页/树形/虚拟定位及双语 Code/Playground；本条目保持未完成，下一步继续这些集成工作。
- 内部基础回归：Table、TableSelect、VirtualList 共 150 项测试通过，组件类型检查通过。尚未宣称校验功能对外完成。

## 数据校验集成验证记录（2026-09-04）

- `validationRules` 与列 `rules` 支持必填、类型、范围、长度、正则和自定义同步/异步校验；声明式列与配置列共用规则。修复内联对象列配置重复注册造成的循环更新，实际规则变化仍会清除旧结果。
- `validationConfig` 显式开启提交前校验，手动 `validateCell`、`validateRow`、`validate` 不依赖编辑开关。单元格模式检查当前字段，整行模式包含未修改的规则字段；失败保留草稿，不发布 editCommit。继续输入、取消、新请求、外部数据替换及卸载使旧请求失效。
- 提供错误快照、定向清理、取消及错误定位方法。全表范围覆盖已提供和已加载的树数据，不请求未加载子节点或远程页；view 范围包含当前页展开、筛选后的行，不只检查虚拟窗口。跨页定位尊重受控分页与展开，隐藏或筛除目标不能定位时返回 false。
- 浏览器确认错误以危险色浅背景、弱阴影和文字显示，无边框。内容和消息独立布局，不挤压输入或树节点缩进；错误 ID 包含表实例、行键、字段和列索引，重复字段列不会产生重复 ID。内置编辑器关联 aria-invalid/aria-describedby，自定义插槽可读取 error/validating。
- 验证跨页异步提交失败后仍保留草稿，定位不会意外取消编辑；键盘 Ctrl+Enter 修正后成功提交。已完成的旧错误在行替换、字段修改后不再显示或参与定位；批次较早字段失效时，不发布过期错误。
- 100 万行 × 10 万列的定点检查可定位第 999999 行、第 99998 列，页面及双语 Playground 均只挂载 13 个窗口行。指定列扫描按需读取，按时间与步数让出执行，AbortSignal 取消后保留先前错误；达到 maxErrors 时明确报告 truncated。上述记录不代表全量扫描吞吐、完整内存或全部组合认证。
- 错误增删会重测虚拟行高；浏览器末端错误行显示时 62px，清除后恢复 42px，没有残留空白。浅色/暗色、普通/虚拟行、固定列与错误消息换行均已检查。
- 三类示例的中英文页面、完整 Code 和 Playground 全部验证，公开配置的类型详情能够展开。Table、TableSelect、VirtualList 回归 160 项通过；随后行高重测与分次错误更新的校验专项 22 项通过。文档示例/API 4 项及组件、文档类型检查通过；源码 include 规范化检查无遗漏。共享 Code/Playground 实现未改动，阶段公共验收项仍未勾选。

## 变更追踪基础验证记录（2026-09-04）

- 新增独立的已接受变更记录模块，按稳定行键合并新增、修改和删除。准备操作不会写入记录；取消或拒绝后不计入已修改数据。新增后编辑仍归为新增，新增后删除抵消，修改回到原值后移除记录。
- 还原计划保留原字段值、属性是否存在及删除位置/相邻行信息；数据所有者接受还原后才清除对应记录。支持单行和全部还原计划；修改后删除仍能恢复修改前的值，未对消费者数据执行原地修改。
- 待接受操作按行键检测竞态，不同行可独立接受。同一行改动后又回到基线也会使旧操作失效；只为仍有待处理操作的行保留临时版本记录。业务确认带版本检查，过期确认不会清除较新的变更；重置和释放会使待处理操作失效。
- 字段路径合并保留缺失容器、显式 undefined、数组长度与空位。生成行只访问声明的改动字段，不枚举行对象；字段快照验证了循环引用、日期和 Map，以及长度十亿但只有一个元素的稀疏数组。这不代表可渲染十亿行或全面容量认证。
- 变更记录与字段还原专项 27 项、连同编辑和生命周期回归共 41 项通过，组件类型检查通过。当前仅完成内部基础，公开增删改/查询/还原 API、编辑与校验联动、普通/懒树/生成源的数据接受流程及双语 Code/Playground 尚未接入；「变更追踪」和「撤销与重做」保持未完成。


## 变更追踪接入验证记录（2026-09-04）

- `changeConfig` 默认关闭。普通数组通过 `v-model:data` 接受不可变提案；新增 `insertRows`、`removeRows`、`updateRow`、`revertChanges`、`getChangeRecords`、`acceptChanges`、`resetChanges` 和 `cancelDataChange`，以及数据提案、已接受变更、记录版本事件。方法以稳定行键定位，插入位置按源同级数组解释，不受排序或分页序号影响。
- 编辑草稿先经过已有校验和数据所有者接受流程，成功后才进入记录并发出 `editCommit`；拒绝保留草稿，取消与卸载中止待接受请求。业务保存后的版本确认与本地应用分开；过期确认不能清除新变更。独立数据 API 不隐式运行编辑校验，文档示例在确认基线前显式校验。
- 普通数据只复制受影响同级数组、行和祖先。树分支还原包含隔着未修改父节点的后代及已删除节点；新增后删除抵消，不会随父分支恢复。懒加载只处理已载入数据，并让提案中的显式子数组覆盖旧懒加载缓存。验证消费者数据不被原地修改、空操作不发数据事件、稳定相邻键保持批量插入和恢复顺序。
- 生成源通过 `indexOf` 和 `apply` 适配，更新读取字段补丁，还原不枚举行对象；删除位置包含相邻稳定键。专项覆盖不允许枚举字段的百万行源、源删除后的索引偏移，以及生成数据的新增/修改/删除组合还原；行读取保持有界，未建立全量行列快照。适配器负责稳定键映射、源行数、只读行版本和写入前检查 AbortSignal。
- 浏览器发现源对象更新会被深度列监听误判为列变化，导致已写入的编辑被取消；源列函数现在按引用变化单独监听，专项验证提交记录不会丢失。只更新源行数据或行数时保留调整后的列宽，更换列定义才重置；异步旧编辑结束也不会清除新会话的应用状态。
- 三组中英文示例（普通数组、懒树、生成源）的页面、完整 Code 和共享 Playground 已验证。检查新增与还原、基线确认、必填校验保留草稿、树后代增删还原、键盘开始/取消、浅色和暗色，以及 API 类型详情。生成源页面和双语 Playground 到达末行 999999、末中心列 99998，修改后记录为 1、还原后为 0，当前窗口挂载 11–14 行。
- Table、TableSelect、VirtualList 完整回归 214 项通过，文档示例/API 测试 4 项通过，组件和文档类型检查通过。源码 include 已规范化，未修改共享 Code/Playground 管线。测试工程的全量类型检查仍有 Card、Carousel 等既有错误；本轮新增测试类型问题另行修正并复核。阶段级完整性能/内存和所有功能组合认证仍未完成，不因窗口计数勾选公共验收项。

## 撤销与重做验证记录（2026-09-04）

- `history-config` 默认关闭，需配合 `change-config` 使用；提供 `undo`、`redo`、`clearHistory`、`getHistoryState` 和 `historyChange`。历史按成功操作分步，整行编辑、批量增删及还原操作各占一步。
- 保存受影响字段补丁、增删行的只读引用和受影响行的稀疏变更记录检查点；不复制整个数据集。默认保留最近 100 步，可配置上限；已删除的大型加载分支仍有相应内存成本。
- 回放复用受控数据接受流程。活动草稿阻止回放；拒绝、异常、取消和卸载不会消耗历史。新操作清空重做分支，无变化写入保留它；确认基线（包括部分行确认）、重置、更换数据基线或关闭历史清空全部历史。单独清空历史保留数据和变更记录。
- 单测覆盖连续编辑、批量增删、可撤销还原、缺失字段恢复、字段冲突、整行校验、懒加载后代、排序分页后的源顺序、生成源增删、异步取消和卸载；百万行十万列的不可枚举行源完成字段回放，仅按需访问。
- 浏览器验证中英文普通表格、懒加载树与生成源，以及全部六个完整 Code / Playground 入口。末行 999999 / 末中心列 99998 的编辑可撤销并重做，挂载约 11–14 行；已检查浅色、暗色、键盘按钮和新 API 类型详情。
- Table、TableSelect、VirtualList 共 229 项通过，文档示例/API 4 项通过；最后的历史与变更 API 定向回归 31 项通过。组件和文档类型检查通过，测试类型检查仍有原有 Card、Carousel 等错误，本次历史相关文件无报错。公共组合及性能验收仍未完成。

## Grid 集成层与查询工具栏验证记录（2026-09-04）

- 新增 `STableGrid`，组合 SForm、工具栏和一个原有 STable。表格属性、事件、声明式列及插槽继续转发；没有复制行渲染、排序、筛选、分页、选择或虚拟滚动实现。
- `query-config` 接受表单模型、字段、校验和布局配置；查询先校验再请求第一页，重置恢复初始字段并请求第一页，刷新保留当前页。事件提供独立条件快照，应用仍自行管理数据请求；后续请求代理尚未实现。
- 查询校验期间阻止重复操作；拒绝受控分页更新、校验过程中条件改变、加载中、禁用或卸载时不发出查询。原生 Enter 提交只经过一次校验；卸载后即使业务校验没有结束，等待中的 Grid 查询也会返回 false。
- 工具栏提供标题、刷新和带 code 的业务按钮，可配置显隐、禁用和加载状态。查询字段、操作按钮和工具栏均可通过插槽替换；列设置复用 STable 原面板。业务行修改和撤销经 getTable 调用原方法，数据仍由消费者接受。
- 14 项 Grid 集成测试通过，覆盖默认模式、配置和插槽、受控分页接受、查询异常、异步边界、排序筛选、数据回放、声明式列、懒加载和稀疏生成数据。此前完整 Table / TableSelect / VirtualList / Grid 回归 241 项通过，随后新增的两项查询边界在 14 项定向回归中通过。
- 中英文三组页面示例与全部六个完整 Code / Playground 入口均验证：本地查询、重置、刷新保留页码、校验、选择业务按钮、树分组收起、列显隐与恢复。浅色、暗色、键盘提交及新 API 类型详情已检查。
- 百万行十万列在页面和双语 Playground 均能到达末行 999999、末中心列 99998；末端挂载 11 行，左右固定列和中心列的顶部位置与高度一致。此记录不是阶段级所有组合或性能/内存容量认证。
- 文档源码 include 已规范化，文档示例/API 4 项测试通过；组件和文档类型检查、Grid ESLint 通过。测试工程全量类型检查仍有 Card、Carousel、Table 等既有错误，新增 Grid 文件已无类型错误。组件注册、按需样式入口、全局类型和中英繁体按钮语言已同步。

## 数据请求代理验证记录（2026-09-04）

- `STableGrid.proxyConfig` 默认关闭，配置 query/save/delete 适配器后统一条件快照、loading、状态、错误和 AbortSignal。提供 commitProxy、cancelProxy、getProxyState；原 query 事件及手动数据管理方式保留。
- 查询使用远程分页/排序/筛选，一页结果不会重复本地处理。默认挂载查询及接受分页、排序、筛选后查询；表单草稿仅显式提交。旧查询、取消、卸载、数据集及适配器更换都使过期结果失效，即使适配器不响应取消，公开调用也能结束。
- 未传 data 时保存内部响应；显式 data 保持受控，必须接受返回数组。失败保留旧数据；查询和删除遇到未保存变更、活动草稿会给出可见反馈。写请求互斥，成功写入与随后刷新失败分别返回，避免重复写入。
- 保存读取原 Table 变更记录，按稳定行键校验新增和修改记录，仅接受同一数据基线和版本。修复 Grid 数据接受时重新创建排序/筛选配置、误触编辑取消的问题；整行草稿现在能完成提交、进入变更记录并保存。
- 生成数据源直接查询仍由业务适配器管理；代理保存要求明确数字校验列与行键索引，只读取受影响行和限定列。百万行十万列不可枚举行源保存通过；无效列索引阻止写入并保留变更。此记录不等于全量性能/内存或所有组合认证。
- Grid 代理 13 项、原 Grid 14 项及 Table/TableSelect/VirtualList 229 项通过（完整回归其余 243 项通过，调整新增边界测试的计数范围后，代理 13 项再次全部通过）。覆盖竞态、异常、拒绝、取消、卸载、编辑保存、版本冲突、删除后刷新失败、树形虚拟页和稀疏生成数据。
- 中英查询与保存共四个新示例的完整 Code / Playground 已核对。浏览器验证保存后重新查询仍保留编辑值、新增保存总数为 4、删除后总数为 2、失败保留数据、重试、取消和翻页。查询预览只挂载 13 行；浅色、暗色和新 API 类型详情已检查。
- 源码 include 规范化无遗漏，文档示例/API 4 项、组件与文档类型检查通过，相关 ESLint 通过。测试工程类型检查仍有 Card、Carousel 等已有错误，本次 Grid 与校验文件无错误。另修复中文历史示例初始 canUndo/canRedo 属性被错误翻译的问题。

## 行拖拽排序验证记录（2026-09-04）

- `row-drag-config` 默认关闭，配置列或声明式列的 `dragSort` 提供手柄。支持 checkMethod/dropMethod、鼠标、触摸指针、Space/Enter 拾取、方向键选落点、Enter 放置、Escape 取消及焦点恢复。公开 moveRow/cancelRowDrag 和 rowDragStart/rowDragEnd，重排反馈使用现有 HSL 表面和弱阴影，无边框。
- 普通数组通过 update:data 或 apply 适配器接受完整提案；只复制受影响的同级数组与祖先，原始行不变。筛选和分页以可见目标在源数组的位置移动，其他页和隐藏行保留。树形仅同级移动，展开后代保持关联；懒加载子节点重排不会修改原父对象或请求未加载节点。
- 排序存在时禁用；活动编辑草稿或未保存变更需先处理。重排建立新的数据基线，不伪装成字段编辑或纳入编辑撤销历史。公开方法使用当前展开页索引，生成源适配器收到正确的绝对源位置，包括分页偏移。
- 生成源必须提供 apply，组件不复制全部行，只提供源/目标行键和位置并校验移动后的行键。异步拒绝、错误、取消、外部数组替换和卸载均有结果；非协作 Promise 取消后也能结束等待。手势与受控数据提交由独立 composable 管理。
- 指针监听不附着在即将卸载的行上；按帧处理命中和边缘自动滚动，仅检查当前 DOM 落点。退出区域、取消、卸载会清除监听与帧任务；详情中的嵌套表格不成为外层落点。
- 完整 Table/Grid/TableSelect/VirtualList 回归 269 项通过，最后增加嵌套表格隔离并修正禁用状态缓存后的行拖动专项 14 项通过。覆盖受控接受/拒绝、键盘、筛选分页、生成源分页、懒树、选择保持、异步取消、卸载和源手柄卸载后的自动滚动。
- 浏览器确认普通鼠标重排、键盘放置/取消/恢复焦点，懒树同级移动及父节点移动后的后代关联。百万行十万列示例把 999998 行经边缘滚动移至 999736；源行已卸载仍可放置，更新 263 个位置映射，挂载 20 行。末行相邻移动仅更新两个位置映射，挂载 13 行。
- 生成源明确配置左右固定列数量，横向滚至中心末列 99998 后拖动手柄仍可用，固定和主体单元格顶部一致、高度均 44px。双语三组示例的全部六个完整 Code/Playground、明暗主题及 API 类型详情均检查；最后补齐固定列配置后重新验证两种语言生成源入口。
- include 规范化已审阅，文档示例/API 4 项、组件和文档类型检查通过。测试工程仍存在原有其他组件类型错误，本次行拖动相关文件无错误。此条目不代表所有阶段组合、全量性能或内存容量认证；生成源示例长距离移动的适配器成本随跨越行数增长，并分批让出执行、支持取消。


## 基础键盘导航验证记录（2026-09-04）

- `keyboard-config` 默认关闭；活动单元格由独立坐标与焦点 composable 管理，`v-model:active-cell` 与行高亮分离。新增 update:activeCell / activeCellChange 及 setActiveCell / clearActiveCell / getActiveCell，配置列和声明式列共享同一行渲染管线。
- 方向键沿左固定、中间、右固定的视觉顺序移动，跳过隐藏列；Tab/Shift+Tab 跨行，页边界保留浏览器原生 Tab 行为，不自动翻页。Enter/F2 可进入编辑器，Escape 取消编辑后恢复单元格焦点；编辑器内部方向键、Tab、弹层按键及 IME 保留原行为。
- 普通数据按稳定行列键跟随重排；翻页、筛选、折叠或隐藏使目标失效时请求清空。受控拒绝不伪造已接受活动状态；异步定位以最新请求为准，卸载和关闭功能清理观察器、文档监听及帧任务。
- 虚拟窗口卸载活动格时暂存焦点到表格入口，只在该格重新挂载且焦点仍属于表格时恢复。外部聚焦/点击及嵌套表格不会被外层抢回。跨双轴远距离跳转会等待目标实际挂载，避免行列分帧更新导致按钮仍持有焦点。
- 生成源通过 rowIndexOf 将稳定键映射到绝对源行位置，列地址使用源索引字符串，不遍历全部行列。浏览器实测 100 万行、10 万列的 999999/99998 定位，实际焦点落到单元格；跨到右固定列及上一行正常，末端挂载 13 行，固定与中心单元格顶部一致、高度 44px。
- 活动格沿用 HSL 主色浅背景与弱内阴影，无边框；已检查明暗主题。中英文普通树形与生成源共四组完整 Code / Playground 均实测，预览里的末端跳转和树子行导航正常，新 API 类型详情已验证。
- 完整 Table、Table Grid、Table Select、Virtual List 回归 280 项通过；最后补充焦点竞态处理后的键盘专项 10 项再次通过。文档示例/API 4 项、组件与文档类型检查通过，新增及变更代码 ESLint 通过。测试工程类型检查仍有 Card 等旧错误，本次键盘导航文件无错误。
- 源码 include 规范化完成且复跑无变更。上述窗口计数与专项验证不等于所有阶段组合或全量性能/内存认证，公共验收项保留待完成。

## 右键菜单验证记录（2026-09-04）

- `context-menu-config` 默认关闭；header/body/footer 接收菜单数组或同步响应式工厂，visibleMethod 控制可见性。上下文区分表头、正文和表尾，保留原始行值、树信息及绝对生成源索引；分组表头提供当前标题段的首个叶子索引。不自动排序、删除或修改业务数据。
- 复用 SContextMenu，并将其定位、视口避让、传送与外部点击统一交给 SPopper。菜单使用 HSL 实色背景、零边框与弱阴影；支持禁用项、分隔项、keepOpen、Shift+F10/菜单键、方向键、Home/End、Escape 与焦点恢复。
- 共享菜单加入 SFocusTrap 的层级，Playground 内传送出的菜单能获得键盘焦点，Escape 只关闭菜单并恢复来源单元格。外部焦点不会被关闭动作抢回；原生编辑器右键和 IME 保持原行为。滚动、查询、数据和列布局变化关闭失效上下文，嵌套表格事件不会冒充外层单元格。
- 新增菜单 13 项测试，完整 Table/Grid/TableSelect/VirtualList/Popper/ContextMenu 回归合计 296 项通过。覆盖默认回退、动态禁用、区域上下文、分组表头、编辑器、嵌套表格、卸载、巨量双轴窗口及嵌套焦点层。组件与文档类型检查、相关 ESLint 通过；测试工程仍有 Card 等旧错误，新增菜单测试类型错误已修正并复查无遗漏。
- 双语普通菜单与生成源的完整 Code / Playground 已核对，独立 ContextMenu 的双语源码与预览也已检查。百万行十万列定位到 999999/99998 后可打开菜单，窗口挂载 13 行；Escape 后仍在预览内并恢复末端单元格。日历原有日期右键菜单仍返回正确日期上下文。
- 检查了明暗主题及窗口右下角避让；普通示例中的查看记录和启动编辑器正常。部分浏览器自动输入超时后的验证使用页面 DOM 事件触发，未把它计作额外的原生指针或系统快捷键认证。上述窗口计数不代表阶段级完整性能、内存或所有功能组合验收。
- 文档 include 规范化复跑无变更；文档示例/API 测试 4 项通过。上一轮下拉编辑器补为块级 flex 布局，一并确认文本、数值、下拉和日期编辑控件顶部及高度一致，单元格 padding 仍为零。
- 重启开发服务后，新 API 类型详情能展开 TableContextMenuConfig、区域工厂、上下文联合类型与 ContextMenuItem。旧浏览器页重载时崩溃一次，新开页面加载与类型详情正常；暂未确定崩溃原因，仍需在阶段级稳定性验收中观察。

## 合并单元格实现进度（2026-09-04，未完成）

- 新增独立区域索引、窗口规则查询、物理几何投影与键盘寻址模块。区域按半开行列坐标查询，先出现的有效区域优先；裁剪超出数据范围的跨度，不修改输入范围。索引按区域保存，不展开被覆盖的单元格矩阵。
- 响应式规则按挂载行及左固定、中心、右固定的连续列窗口查询；必须返回与窗口相交的完整区域，包括视口外起点。静态范围在窗口移动时复用索引，程序定位可单独查询一个未挂载位置。
- 新增按帧合并通知的几何观察器，读取实际挂载行列，支持分页索引偏移、祖先缩放、表尾独立纵向范围和尺寸变化。排除嵌套表格及合并层自身；禁用、换根节点、卸载时清理监听、观察器和帧任务。
- 内部合并层已能按窗口裁剪巨量区域，分开固定列、中心列与详情间隙。一个区域只挂载一个交互内容；改变窗口或主片段位置时保留相同输入 DOM 和焦点，其余片段提供原区域的点击、双击及右键上下文事件。正文和表尾继续复用既有行组件，原单元格可切换为无内容的宽度占位，保留视觉列位置；表尾单独保留原始行索引、格式化和事件上下文。
- 合并专项 29 项及完整 Table/Grid/TableSelect/VirtualList/Popper/ContextMenu 回归共 325 项通过，组件类型检查与 Table SCSS 编译通过；本次相关 ESLint 通过。单测的巨量坐标和窗口数量不等于真实浏览器容量或性能认证。
- 已接入 `merge-config` 公开配置，普通行、虚拟行及合并起点共用行组件和插槽回退。正文、表尾范围独立；关闭后恢复原单元格。修复声明式列内联函数反复注册的循环，稳定转发器仍能响应回调替换。
- 活动地址、方向键、Tab 与程序编辑归一到原区域起点；受控地址需要接受更新，拒绝时不伪造活动状态。生成源的跨页区域按当前页可见边界导航，事件和编辑保留绝对原始行列。合并规则变化遵循现有编辑列变更策略；鼠标双击和 F2 编辑视口外起点时不再滚回原点。
- 合并内容的自然高度按逻辑跨度分配到行，排除装饰和嵌套表格；同一布局的窗口切换保留最大值，真实内容、列布局或 `measure()` 可以重新测量。修复英文表尾换行裁切；内容超过整个视口时在合并单元格内滚动，Page Down 可到达末尾。活动样式切换不会误清除已保留高度。
- 浏览器已核对中英文基础、生成源共四组完整 Code / Playground，以及明暗主题和新 API 类型详情。100 万行、10 万列末端活动地址为 `999996 / 99992`，双语预览均挂载 13 行，向左可到相邻区域。多行内容使末端行从 44px 增为 108px，缩短后回到 44px且保持末端位置；上述计数不代表容量、内存或全阶段组合认证。
- 接入后的完整 Table/Grid/TableSelect/VirtualList/Popper/ContextMenu 回归 339 项通过；后续编辑定位调整后交互专项 35 项通过。最终合并集成 12 项通过，包含排序、筛选、懒加载树、生成源跨页、受控地址、视口外起点编辑和固定列续接点击/双击/右键；自然高度专项 5 项通过。组件与文档类型检查、SCSS 编译、文档示例/API 4 项通过；测试工程仍有 Card、Carousel、旧 Table 测试等已有类型错误，新合并文件未出现类型错误。
- 仍待完成真实浏览器连续双轴滚动、拖动列宽与动态内容的组合检查，以及合并编辑和详情展开的进一步浏览器验证。第四阶段两个合并条目保持未勾选；本轮工作尚未提交，不将当前专项结果视为完整交付。

## 合并单元格交付验证记录（2026-09-04）

- 本记录补齐上一节待验证项。数据区、表尾的静态范围及同步窗口规则进入共享渲染管线；普通数据按排序、筛选、分页、树展开后的可见位置解释，生成源保留绝对索引。越界裁剪、重叠优先级、异常规则和受控活动地址已覆盖。
- 合并区域跨固定列和详情展开时分段绘制，每个区域只渲染一个内容所有者，续接片段转发点击、双击和右键；编辑写入原起点，详情输入与合并编辑相互独立。虚拟窗口之外的起点仍可显示、定位及编辑。
- 真实滚轮检查发现合并层位于原滚动容器外，会把滚轮传给页面；正文片段现传送到原生滚动窗口内，表尾维持原层。复用现有滚动实现，未新增一套滚轮或触摸监听。已验证原生事件路径、位置换算、编辑焦点及卸载清理。
- 固定列宽变化现在保持横向逻辑列及列内偏移，末端继续贴合末端；稀疏改宽、普通变宽、尚未提交的滚动帧和卸载后的延迟写入都有专项测试。内容在视口外变更后重新出现会使旧高度失效，避免保留不再需要的高度。
- 中英文浏览器均完成连续双轴滚动、末端跳转、多行内容增删和真实拖宽。100 万行 × 10 万列场景连续滚动挂载 20 行；固定列 140px → 230px 后仍显示 999996 / 99992 区域，物理横向位置 99516 → 99606；缩短内容后当前窗口所有行恢复 44px。区域规则和测量缓存按已查询/测量区域工作，不构造全量单元格矩阵；持续访问的测量记录仍有累积内存成本，本次不是完整容量或内存认证。
- 新增合并编辑/详情示例及更新的生成源示例，全部四组中英文 Code 和 Playground 已验证；完整源码可编译，预览能够编辑保存或定位末端。基础合并示例的四组先前检查仍有效，本轮仅规范化英文数据的格式及源码行号。明暗主题、键盘和 API 类型详情沿用上节已完成的验证。
- Table、TableGrid、TableSelect、VirtualList、Popper、ContextMenu 共 348 项 / 43 文件通过；文档示例/API 4 项通过，组件与文档类型检查通过。新增列滚动锚点 4 项覆盖上述布局修正。两个合并条目已完成，后续分组、区域选择、剪贴板、查找及图表仍待实现；阶段公共验收项保持未勾选。
- 最终复核修正了一处测试断言的类型用法，合并层 6 项重新通过。测试工程全量类型检查仍有 Card、Carousel、旧 Table/VirtualList 和 utils 测试的既有错误；本次合并与列滚动锚点文件无类型报错。最终 ESLint、差异空白检查和规范化后的文档测试通过。

## 行分组与聚合基础进度（2026-09-04，未完成）

- 新增独立聚合、分组模型、显示索引与受控展开模块，尚未接入 STable 公共属性、模板、事件和暴露方法。此项保持未勾选，当前基础代码未提交。
- 本地分组按当前页已排序、筛选并展开的树根分支构建嵌套组；同一根的可见后代保持连续，原行和 TableFlatRow 不被修改。键使用字段路径及带类型的值编码，区分空值、数字/字符串及特殊字符；日期按时间分组，对象字段可提供值归一化函数。
- 分组小计统计该组提供的全部成员，折叠不改变统计结果。整体汇总可选当前页或筛选后的已提供可见树行；自定义回调保持原行索引。内置 count 统计行数；sum/average/min/max 仅统计有限数字，不强制转换字符串，空结果和数值溢出有明确定义。自定义聚合采用独立流式累加器，不收集全量单元格值数组。
- 远程模式接受应用提供的有序分组范围和统计结果，校验范围、父子关系、唯一键及循环引用，不扫描生成源。显示索引由数据段、组标题和小计组成，折叠组不进入数据行坐标；提供逻辑数据索引、源索引、虚拟项索引互转及跳过折叠范围的方向定位。十亿行范围的专项仅构建少于 10 个索引段，不代表实际渲染容量。
- 展开支持默认状态、受控接受/拒绝、已知键去重、嵌套状态保留、配置重建、模式切换和卸载失效。异常配置/聚合会报告错误并保留原数据顺序；生成源误用本地分组会报错而不枚举行。
- 基础专项 37 项 / 3 文件通过，组件类型检查通过。测试工程仍有先前的全量类型错误，本次分组与聚合文件无报错。尚未验证分组 UI、固定列/动态虚拟行、分页编辑选择合并联动、中英文 Code/Playground，以及远程请求示例；需完成这些接入和组合验证后再交付此条目。


## 行分组与聚合交付验证记录（2026-09-04）

- 本记录补齐上一节基础阶段的接入与验证。`group-config` 默认关闭，本地嵌套分组、受控展开、小计和整体汇总已经接入 STable；公开 `group-expanded-keys`、展开/错误事件、查询和设置分组的暴露方法。普通与虚拟正文共用组合组件，原行渲染、详情、列声明和消费者数据所有权不变。
- 本地按当前页的排序、筛选与树展开结果分组，根分支及其后代保持连续。组小计不随分组收起改变，整体汇总可选当前页或筛选后的已提供记录；内置有限数值统计与自定义流式累加器共用聚合模块。类型化稳定键、空值、溢出、重复定义、异常回调、受控拒绝和卸载都已有覆盖。
- 远程接口接收有序组范围、组小计和整体汇总；普通页范围为页内索引，生成源为绝对索引。新增请求示例复用 STableGrid 的分页、失败与取消处理，以 WeakMap 将元数据关联到已接受的数据页；失败或取消后原行与汇总保留。组件不自行发起全量请求或扫描生成源。
- 收起后的数据坐标与组标题/小计虚拟坐标独立。补齐活动格清理、隐藏成员编辑保护、提交/取消草稿策略、树懒加载和原始行索引；合并区域可跨越收起组，隐藏起点仍可通过可见续接区域导航及编辑。
- 修复 SVirtualList 的延迟测量竞态：记录测量元素和稳定键，索引换键、移除、越界或卸载后丢弃旧测量，旧 ref 清理不会移除新元素。专项测试证明旧行 120px 测量不会污染同索引新行的 44px 高度，保留原生成源的按需读取预算。
- 中英文普通分组、远程请求、百万行生成源共六组完整 Code / Playground 均验证；源码 include 规范化复跑无改动。请求示例检查分页、失败、取消和汇总，生成源预览检查 999999 / 99998 末端定位；新 API 类型详情在重建开发服务索引后两种语言均可展开。
- 真实浏览器检查键盘 Enter 展开/收起、明暗主题、隐藏活动格清理及连续双轴滚轮。百万行、十万列场景固定列 140px → 210px 后仍对齐；末端挂载 14 项，连续滚动挂载 17 行，固定与主体单元格顶部和高度一致。索引按组段存储；这些窗口计数不代表所有阶段组合或全量内存、性能、容量认证。
- 最终 Table、TableGrid、TableSelect、VirtualList、Popper、ContextMenu 回归 400 项 / 48 文件通过；文档示例/API 4 项通过，组件与文档类型检查、相关 ESLint、差异空白检查通过。测试工程仍有原 Card、Carousel、旧 VirtualList 等类型错误，本次新增分组、聚合、合并组合与测量用例未引入类型报错。
- 此条目已完成。后续区域选择、剪贴板、查找替换、图表及阶段公共验收仍未完成，继续逐项实现；不执行网站发布。


### 第四阶段：单元格区域选择（2026-09-04）

- 新增显式开启的 `range-config` 和受控 `v-model:cell-range`，端点使用稳定行列键；提供设置、清空、读取端点与半开逻辑边界的方法，以及接受变更和解析错误事件。活动单元格、行高亮与区域选择保持独立。
- 鼠标拖选按帧命中，支持 Shift 点击、Shift 方向键、Home/End、Ctrl/Command + A、Escape 清空及拖动取消恢复；避让编辑器、输入法、行拖拽及外部焦点。受控拒绝不伪造高亮，过期异步计算不发布结果或抢焦点。
- 固定列和虚拟列共用视觉逻辑坐标；相交合并单元格扩展到完整区域，跨分组/分页时只计可见数据行。范围查询按新覆盖条带推进，不枚举单元格矩阵；批次间让出执行并支持取消。生成源保留端点索引提示，视口外程序寻址可提供 `rowIndexOf`。
- 边缘滚动复用横向虚拟滚动与 VirtualList 逻辑像素通道。新增 `SVirtualList.scrollBy`；横向压缩轨道累积被原生像素取整丢弃的小步进，避免持续拖动却不滚动。合并续接片段释放时聚焦最新可见位置，取消替换拖选可恢复原生成源端点。
- 合并配置按内容稳定，等价的内联配置重建不会取消受控拖选或重置测量；真实配置变化继续重算。范围使用主色 HSL 淡背景和弱阴影，固定列与中心列一致；新增中英文辅助技术状态播报与单元格选区描述。
- Table/Grid/TableSelect/VirtualList 回归 466 项 / 53 文件通过，文档示例/API 4 项通过；组件和文档类型检查通过，相关 ESLint 与差异检查通过。测试工程仍存在此前的 Card、Grid 等类型错误；新增区域选择测试的类型错误已单独修正。
- 中英文普通/巨量共四组 Code 和 Playground 完整源通过浏览器验证，新增 API 类型详情双语可展开。原生拖选覆盖普通合并区域、双轴边缘滚动及右固定列；生成源边缘拖选形成 11 × 100000 逻辑区域，固定/中心非活动选区背景一致。明暗主题均已检查。全表选区为 1000000 × 100000，末端仍仅挂载可见窗口（约 12 行）；这些记录不构成完整容量或内存认证。
- 本条目完成；复制/剪切/粘贴、查找替换、图表与阶段公共验收仍待实现。不执行网站发布。

### 第四阶段：剪贴板数据基础（2026-09-04，未完成）

- 新增内部批量更新计划，复用原数据所有者接受、变更记录和历史管线。原 `updateRow` 已走同一实现；多行更新只发出一次数据提案和一次批量 `dataChange`，作为一步撤销/重做。任一行无效、稳定键改变、字段基线冲突或所有者拒绝时，不接受部分记录。
- 批量入口支持调用方 AbortSignal 与有效性检查。取消和卸载能结束对不响应请求的等待，并把中止信号交给适配器；已经被不遵守信号的适配器写入的数据无法由组件强行回滚，过期结果不会发布成功或历史。
- 新增纯 TSV 编解码，保留制表符、引号、多行文本、Unicode 和尾部空行；不解释 HTML 或公式。按完整矩形计算补齐成本，默认最多 10000 个单元格、2000000 个文本字符；超限明确拒绝，配置必须为正安全整数。
- 逻辑区域读写计划通过坐标适配器按需访问，尚未连接 STable 交互。合并区域只复制一次，续接位置留空；粘贴保留只读格位置，合并格或重复字段收到冲突值时拒绝整份计划。目标尺寸必须为来源矩形的整数倍，支持单值填充和整块重复；不隐式截断或新增行。
- 生成完整的每行候选草稿、字段快照和补丁，供后续编辑校验与批量提交使用；按步数和时间让出执行，可中止永不返回的调度等待。二维输入在异步工作前独立快照，转换回调之间不共享可变输入；复制后可检查源字段是否仍与快照一致。
- 新增 28 项专项测试；Table、TableGrid、TableSelect、VirtualList 共 494 项 / 56 文件回归通过。组件类型检查和相关 ESLint 通过；测试工程仍有原 Card、Grid、utils 等类型错误，本轮新增测试类型问题已修正并复核。末端小区域验证未枚举百万行十万列来源，巨量全选在读取前拒绝超限；这些是内部数据层证据，不代表浏览器交互或完整容量认证。
- 下一步：接入公开配置、方法与事件，复用编辑只读条件及异步校验，完成复制/剪切/粘贴快捷键和浏览器 Clipboard API，明确剪切清除与撤销策略，再补双语示例、Code/Playground 和真实剪贴板验证。原生剪贴板事件与异步 API 的生命周期需分开处理，参考 [W3C Clipboard API and events](https://www.w3.org/TR/clipboard-apis/)。本条目继续保持未完成，不执行网站发布。


### 第四阶段：复制、剪切与粘贴（2026-09-04）

- 新增显式开启的 `clipboard-config`、`clipboard` 结果事件，以及 `copyCells`、`cutCells`、`pasteCells`、`cancelClipboard`。原生复制/剪切/粘贴事件与编辑器自身文本操作分离；无选区时使用活动单元格，单格目标可以按输入矩形扩展。TableGrid 复用表格配置和事件，通过 `getTable()` 调用方法。
- 支持 TSV 和独立快照的二维数据；普通、固定、树形、分页和生成源共用逻辑坐标与合并区域解析。只读格保留原位置，重复字段的冲突值和不完整合并区域拒绝整批；相同字段的不同列规则全部受约束，后续通过的列不再清除前一列的错误。
- 写入复用编辑条件、候选行校验和数据所有者接受管线；成功批量操作只有一次数据提案、一次批量变更和一步撤销/重做，不直接修改消费者行对象。数字、布尔、选项值及 Date 编辑器提供基础文本转换，可使用自定义转换。
- 剪切先完成复制，再校验和清除可写字段；若校验失败，明确返回已复制但源数据未清除。剪切清除和后续粘贴各占一步历史。支持调用信号、表格焦点下 Escape、配置/数据变化及卸载取消；过期结果不会覆盖新反馈。系统剪贴板写入不能撤回，取消时仍未确认的 OS 写入通过 `clipboardWritten: null` 表示。
- 中英文普通与百万行 × 十万列生成源示例均补齐完整 Code 与 Playground，四组入口已在真实浏览器验证，类型详情两种语言均可展开。原生 Ctrl+C/V/X、校验失败的剪切、撤销、分组与动态虚拟滚动粘贴、末端跨右固定列合并格及明暗主题通过检查；测试浏览器授予真实 Clipboard 权限，未替代系统剪贴板 API。
- 全表选区在访问数据前拒绝超限；默认上限 10000 单元格和 2000000 文本字符。末端示例只挂载可见窗口；区域读取、候选行生成和校验分批让出执行，取消可终止等待，不加载远程未提供的页面或树节点。此证据不代表全量内存、容量或阶段组合认证。
- Table、TableGrid、TableSelect、VirtualList 回归 511 项 / 57 文件通过；最后补充重复字段校验后，剪贴板与校验专项 59 项 / 5 文件通过。文档示例/API 4 项、组件与文档类型检查、相关 ESLint、源码区间规范化通过。测试工程类型检查仍有既有 Card、Grid、utils 等错误，本轮剪贴板及共享校验修改未新增类型报错。
- 此条目完成；查找替换、可选图表适配层与阶段公共验收仍待完成。不执行网站发布。


### 第四阶段：查找与替换数据基础（2026-09-04，未完成）

- 新增内部按需扫描器和替换计划。支持字面文本、大小写敏感与整格匹配，保留 Unicode 和多次命中；不会把查询或替换内容解释为正则表达式、捕获组或公式。非标量数据可提供格式化函数，缺省不搜索对象表示。
- 范围由惰性单元格解析器提供，扫描器在工作量超限后不再读取后续格。默认最多扫描 100000 个坐标、保留 1000 个匹配格、处理 2000000 文本字符；结果明确标记不完整及上限原因，不完整结果禁止隐式“全部替换”，显式选择其中单项仍可生成替换计划。
- 合并续接坐标按稳定行列键去重；只读、禁用和业务条件限制写入。替换先检查输出文本膨胀成本，再构造文本和类型转换；不会因为短文本的大量重复匹配分配超限字符串。相同字段整合为一个补丁，同时保留各列校验上下文，冲突值拒绝整个计划。
- 抽取共享候选行计划、基础文本类型转换和可取消的分批调度，现有剪贴板继续复用这些实现。候选行保存字段基线和完整草稿，供下一步接入校验、所有者接受、一次批量变更及一步撤销；此阶段本身不写数据。
- 来源变化、转换期间的字段变化、范围失效及 AbortSignal 都会终止旧操作。扫描和替换准备能取消不响应的调度等待；巨量范围只读取预算内坐标，末行的小范围不枚举前面行。
- 新增 15 项查找/替换数据测试；Table、TableGrid、TableSelect、VirtualList 完整回归 528 项 / 58 文件通过，组件类型检查及相关 ESLint 通过。测试工程仍有既有 Card、Grid 等类型问题；本次新增与抽取模块未引入类型错误。
- 尚未提供公共查找配置/方法、搜索面板、匹配导航、搜索范围适配、校验提交集成、虚拟定位和双语 Code/Playground。下一步完成这些交互与组合验证；“查找与替换”保持未勾选，不将内部数据测试视为整项验收。


### 第四阶段：查找与替换交互接入（2026-09-04）

- 新增显式开启的 `find-config`、搜索面板、`findChange` / `replace` 事件及十个独立方法：findCells、findNext、findPrevious、replaceMatch、replaceAll、getFindState、clearFind、cancelFind、openFind、closeFind。TableGrid 继续转发原表格配置与事件，通过 getTable 调用方法。
- 支持当前展开筛选页、矩形选区和全部已提供/已加载数据三种范围，可限定列或显式半开坐标。视图和选区复用合并格坐标并去重所有者；已提供数据按可见列原字段搜索，不请求远程页或未知树节点。重新框选使旧选区结果失效，显式 bounds 独立于当前框选。
- 匹配导航复用树祖先展开、分组展开、分页及双轴虚拟定位；受控视图拒绝切页、隐藏列或筛选隐藏行时返回定位失败，保留有效数据范围匹配。普通和生成源均可循环导航；面板导航保留输入焦点，替换后重新搜索并同步活动格。
- 单项/批量替换复用编辑可写条件、原字段快照、完整候选行校验和所有者接受流程。一次成功批量写入对应一次提案、一次变更和一步历史；校验失败、只读、拒绝、过期、取消、卸载均不部分提交。数据范围的树状态供业务可写条件读取，不伪造全部为折叠状态。
- 默认扫描上限为 100000 个坐标、1000 个匹配格和 2000000 文本字符；扫描与准备按预算分批让出执行。空列范围无需访问生成行，零匹配但未扫描完仍返回 limit，禁止全部替换。取消可结束不响应的异步校验等待；已接受外部写入只能通过历史撤销。
- 面板使用仓库输入、选择、复选框和按钮，支持 Ctrl/Command+F、Ctrl/Command+H、F3/Shift+F3、Enter、Escape 和焦点恢复。新查询或结果失效会清除旧替换提示，避免遮挡上限和新结果。已检查中英文、明暗主题和 390px 窄屏布局。
- 两组中英文页面、完整 Code 和共享 Playground 四个入口均实测替换与撤销；直达 Playground 入口与 Code 转入入口一致，公开配置类型详情两种语言均可展开。浏览器验证跨页查找、必填校验失败、分组和动态虚拟表、末行 999999/末列 99999 跨右固定列合并格、4096 扫描上限及固定列对齐；生成源仅挂载可见窗口。这不是阶段级全量吞吐、内存或所有组合认证。
- 完整 Table / TableGrid / TableSelect / VirtualList 回归 545 项、59 文件通过；随后面板提示与选区失效补充后的查找/校验专项 46 项、3 文件通过。文档示例/API 4 项、组件与文档类型检查及相关 ESLint 通过，源码区间规范化已审阅。测试工程全量类型检查仍有既有 Card、Grid、utils 等错误，本轮查找及抽取模块无新增报错。
- 此条目完成。下一项为可选图表适配层；公共架构、性能/内存和阶段组合验收仍保持未完成。不执行网站发布。


### 第四阶段：图表数据基础（2026-09-04，未完成）

- 新增独立的图表快照与范围解析模块，不导入图表引擎，也未改变普通表格的默认渲染。输出为冻结的分类、稳定点地址和数值序列；相同分类标签保留为不同点，不隐式合并。缺失、非数值、NaN/Infinity 默认保留为 null 间隙，不自动填零或转换数字字符串；显式转换回调接收字段独立快照与点/序列上下文。
- 支持把选区、传入的分页前筛选模型、根分组/指定子分组及整体汇总解析为同一惰性数据源。范围解析复用原有列/行模型、合并格查询和聚合结果，不复制过滤、分页、树展开或聚合计算。聚合路径不访问成员行；未提供的远程数据不会被请求。
- 选区中的数值合并格仅在所有者位置计数，续接坐标为 null；分类标签可延续合并内容。不完整合并区域、不可见列、越界映射和未知分组键明确拒绝。合并查询按可处理行数及实际映射列跨度收窄，不先展开巨量选区。
- 默认上限为 1000 个数据点、32 个序列、10000 个分类/数值坐标和 2000000 个标签/地址/序列文本字符。先预留完整点预算再调用行解析器，不发布半个数据点；达到上限返回明确的不完整状态。元数据超限在扫描前拒绝，点地址长度在构造身份字符串前检查。
- 数据生成与最终字段一致性核对均分批让出执行，支持 AbortSignal 和范围版本校验。转换期间字段改变、过期行/聚合模型或取消会终止，不返回部分成功图表。冻结输出不保留原始行对象；内部字段快照与回调处理的复杂对象仍有相应内存成本，文本/坐标上限不代表完整堆内存限制。
- 新增 24 项快照/范围测试；完整 Table、TableGrid、TableSelect、VirtualList 回归 571 项 / 61 文件通过。覆盖预算边界、生成行按需字段读取、跨右固定列合并、分页前筛选、子分组/汇总、不可变转换、取消不响应调度、数据冲突及序列定义快照。组件类型检查和相关 ESLint 通过；测试工程仍有既有其他文件类型错误，本次图表文件无新增类型错误。
- 尚未接入公开 chart-config/方法与事件、图表展示适配器及其挂载/更新/尺寸/释放生命周期、可访问的展示面板、中英文文档和 Code/Playground。下一步完成这些集成及真实浏览器的固定列/虚拟滚动组合验证，再验收「图表集成」；本条目保持未勾选，阶段公共验收仍待完成，不执行发布。


### 第四阶段：图表集成交付（2026-09-04）

- 本记录补齐上一节数据基础的公开接入。`chart-config` 默认关闭；公开 `getChartData`、`openChart`、`closeChart`、`cancelChart`、`getChartState`，并补齐 `chartChange`、`chartError` 及 TypeScript 类型。取数不依赖绘图库；展示层通过显式 `adapter` 接入，按需导出的 `createTableSvgChartAdapter()` 提供柱状图与折线图，没有新增强制图表依赖。
- 选区、分页前筛选结果、现有分组和整体汇总进入同一快照管线。完整合并值只计一次，固定列参与可见列映射；普通树范围使用已加载、已展开记录，聚合范围沿用已有配置，远程统计不枚举组成员。生成源使用明确的行键寻址；预算含类别和所有系列，超限保留明确的不完整结果，面板不会展示截断图表。
- 异步取数支持取消与失效校验，数据/视图/列/分组变化取消旧请求；生成源原对象的取行函数、键函数、列函数或计数变化也会失效。等价 chart-config 对象不会中止扫描；已打开图表刷新时保留上一快照。消费者行数据不被修改。
- 可选适配器接收独立容器、冻结数据、解析后的主题色及 AbortSignal。类型/数据/主题变化通过重新挂载更新，旧容器隔离晚返回的写入；关闭、卸载及迟到句柄均释放资源。尺寸更新按帧合并，挂载失败可重试并保留数据表格入口。原生 SVG 覆盖缺口、正负数、零值和极端有限数，标签按文本渲染。
- 展示面板复用 SDialog、SFocusTrap、SButton 和 STable，支持图表类型、数据视图、Tab 循环、Escape、关闭后的焦点恢复及滚动锁。焦点在关闭动画结束后恢复，Escape 不会连带关闭外层 Playground；明暗主题跟随 HSL 来源，390px 窄屏已检查。
- 完整 Table/TableGrid/TableSelect/VirtualList 回归 588 项通过。后续补充末端选区、刷新、树/聚合范围和生成源原对象失效测试后，最终图表专项共 45 项分别通过（数据/范围 24 项、适配器 9 项、接入 12 项）。组件与文档类型检查、相关 ESLint、文档示例/API 4 项通过；测试工程全量类型检查仍有 Card 等已有错误，图表文件无新增类型错误。
- 中英文普通/生成源共四组完整 Code 与两种 Playground 入口均检查，预览能够打开真实图表并保持外层焦点层。普通页面验证选区、筛选、根分组、汇总、数据表格与焦点恢复；百万行十万列末端跨右固定列读取五点，窗口挂载 15 行，超限与远程聚合反馈正常。这些窗口计数不是完整性能或内存容量认证。
- 双语 API 类型详情包含适配器、处理上限与转换上下文。开发服务的旧类型索引经重启刷新；中文类型弹层在页面稳定后通过 DOM 点击展开并核对，未把前两次定位超时计作成功。源码 include 规范化复跑无变更，未修改共享 Code/Playground 管线，也未执行发布。
- 图表集成条目已完成；模块公共约束、跨阶段组合、性能/缓存/内存和完整文档审查等公共验收项仍待逐项核对，不将本次专项通过视为整个 todos.md 已完成。
