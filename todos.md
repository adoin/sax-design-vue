# Table 后续功能计划

状态：已按用户指令开始逐项实现。基线提交为 `6db6956`；完成项仅在对应实现、文档和验证通过后勾选。

已完成：第一阶段「拖动调整列宽」「列管理面板」「多级表头」「表尾数据行」「详情展开行」，第二阶段「单元格与整行编辑」「编辑生命周期」「数据校验」「变更追踪」。下一项为「撤销与重做」；阶段整体验收和其他条目仍待完成。

## 范围

- 以现有 `STable` 为基础，补齐已讨论的 VXE Table / Grid 能力，不另起一套表格渲染实现。
- 明确不做：表格导入、导出、打印，以及对应的工具栏入口。本排除项不要求删除项目已有的独立组件。
- 区域选择、复制粘贴、查找替换、聚合和图表也纳入后续计划；它们属于较后的阶段，不因 VXE 的版本划分而遗漏。
- 以下模块名称和配置设计仅用于规划，正式 API 在对应阶段实现前确定。

## 当前基础（保留，不重复建设）

已有 `data + columns` / `STableColumn`、内置分页、跨页选择、单列与多列排序、列级比较器、本地与远程筛选、树形与懒加载、固定列、双向虚拟滚动、动态行高、渲染器和溢出提示。

保留数据驱动架构，不恢复 `STr` / `STd` / `STh`；保留消费者行数据的所有权，不为索引向原始数据添加内部字段。现有压力示例不等于所有功能组合都已通过容量认证。

## 第一阶段：通用展示与列管理

- [x] **拖动调整列宽**：支持拖动、最小宽度限制和宽度变更事件；维护固定列偏移、横向虚拟列窗口与动态行高缓存的正确性。
- [x] **列管理面板**：支持列显隐、列顺序调整、左右固定设置、恢复默认；支持可选的配置持久化及外部受控状态。
- [x] **多级表头**：配置式和声明式列共享分组表头模型；支持嵌套分组、跨列标题及叶子列排序/筛选，处理显隐、固定列和横向虚拟滚动。
- [x] **表尾数据行**：支持一行或多行表尾、格式化与自定义渲染；表尾与数据列对齐，共享宽度、固定列及横向滚动，不以普通 `footer` 插槽代替。
- [x] **详情展开行**：独立于树节点展开，可放详情、表单或子表格；支持受控展开、键盘操作、异步内容和动态高度测量，兼容固定列及虚拟滚动。

## 第二阶段：编辑、校验与变更管理

- [x] **单元格与整行编辑**：配置编辑器、编辑触发方式、可编辑条件、进入/提交/取消编辑事件；复用项目已有输入、选择和日期等控件。
- [x] **编辑生命周期**：管理活动编辑单元格、焦点与草稿；明确翻页、排序、筛选、滚动卸载及外部数据更新时的提交/取消策略。
- [x] **数据校验**：必填、类型、自定义同步/异步规则，单元格/行/全表校验，错误展示与滚动定位，防止过期异步校验覆盖新结果。
- [x] **变更追踪**：记录新增、删除和修改的数据，提供查询变更、插入、删除、还原单行/全部的能力；区分编辑草稿和业务已确认数据。
- [ ] **撤销与重做**：在稳定行键基础上定义编辑、增删操作历史及清空时机，避免对巨量数据无条件复制完整快照。

## 第三阶段：Grid 业务集成与表格交互

- [ ] **Grid 集成层**：组合查询表单、工具栏、现有分页和 `STable`；只负责业务编排，复用同一个表格渲染、排序、筛选和选择管线。
- [ ] **数据请求代理**：统一查询、刷新、保存、删除与分页/排序/筛选参数；处理 loading、异常、请求竞态及可取消请求，保留手动管理数据的方式。
- [ ] **查询表单与工具栏**：支持配置和插槽、查询/重置、刷新、列设置及业务操作按钮，不添加导入、导出、打印入口。
- [ ] **行拖拽排序**：支持拖拽手柄、禁用条件、变更事件与受控数据更新；明确排序、筛选、分页及树形下的行为，并处理虚拟窗口边缘自动滚动。
- [ ] **基础键盘导航**：活动单元格与选中行分离；支持方向键、Tab、Enter、Escape、焦点恢复及跨虚拟窗口定位，不干扰编辑器和中文输入法。
- [ ] **右键菜单**：支持表头、数据单元格和表尾上下文，动态菜单与禁用条件，键盘唤起与关闭；复用共享弹层和菜单组件。

## 第四阶段：复杂数据布局与 Excel 式操作

- [ ] **跨行/跨列合并**：支持声明式合并及计算规则，覆盖数据区和表尾；明确合并区域与排序、筛选、分页的关系。
- [ ] **虚拟合并**：当合并起点在视口之外时仍正确显示；解决跨固定列边界、横向/纵向窗口切换、动态行高及命中定位，不能仅支持普通小表格。
- [ ] **行分组与聚合**：支持按字段分组、展开/收起、分组小计及整体汇总；支持内置与自定义聚合函数，明确统计范围和远程计算接口。
- [ ] **单元格区域选择**：支持鼠标及键盘选择区域、边缘自动滚动；统一固定列和虚拟列的逻辑坐标，处理合并单元格。
- [ ] **复制、剪切与粘贴**：支持区域文本/二维数据、只读单元格、编辑校验及批量变更事件，明确取消与撤销策略；剪贴板操作不等于文件导入/导出。
- [ ] **查找与替换**：支持匹配导航、单项/批量替换、搜索范围及只读限制，并与变更追踪、校验、虚拟定位衔接。
- [ ] **图表集成**：从选中区域、筛选结果或聚合结果构造图表数据；采用可选适配层，避免为普通表格强制引入图表依赖。

## 模块边界与公共约束

- [ ] 列布局、分组表头、表尾、详情展开分别组织；编辑、校验、变更历史、区域选择和请求代理采用职责单一的 composable，不继续堆入 `table.vue`。
- [ ] 配置式列与声明式列进入同一套模型和渲染管线，保留既有单元格渲染优先级。
- [ ] 默认行为不变，新能力显式开启；新增受控状态、事件和暴露方法同步补全 TypeScript 类型。
- [ ] 新增弹层统一复用 `SPopper`；辅助交互复用仓库现有组件，遵循当前主色、阴影、形状、暗色和禁用态风格。
- [ ] 虚拟渲染仅处理可视窗口与预渲染区；固定列与主体使用一致的行高和坐标，避免错行、内容透出及横向滚动时高度回缩。
- [ ] 列宽或内容改变时明确测量缓存的失效规则；区分同一布局下保留行最大测量高度与真实布局变化后的重新测量。
- [ ] 巨量数据操作明确本地/远程处理范围、内存成本和取消机制，不把浏览器 Map 容量当成表格可用行数保证。

## 各阶段验收清单

- [ ] 单元测试覆盖默认行为、边界、受控更新、异常、卸载清理及键盘操作。
- [ ] 组合测试覆盖普通数据、分页/跨页选择、树形/懒加载、固定列、双向虚拟滚动、动态行高；新能力不能只在孤立示例中有效。
- [ ] 巨量场景记录初始化、连续滚动、跳转、窗口渲染数量、缓存和内存表现；主动注明尚不支持的功能组合。
- [ ] 同步中英文示例文案、渲染示例、完整 Code 源码与 Playground；例子说明和控制均放在同一个 `<card>` 内。
- [ ] 更新逐项 API 文档与项目类型详情，运行 `pnpm run normalize:doc-examples` 并审核结果。
- [ ] 运行相关组件测试、`pnpm run test:docs-examples` 和类型检查；共享文档管线变更另运行 `pnpm run docs:build`。
- [ ] 浏览器检查中英文 Code/Playground、明暗主题、键盘焦点，以及每项新功能与固定列/虚拟滚动的组合。

## 列宽调整验证记录（2026-09-04）

- `resize-config` 默认关闭；配置列与声明式列共用实现。支持受控 `column-widths`、取消拖动、最小宽度、键盘调整和卸载清理，消费者数据及列配置保持不变。
- 拖动提示默认隐藏，鼠标悬浮、键盘聚焦或拖动时出现。浏览器测量确认热区中心与真实边界重合；右固定列调整 120 → 160px 后表头、数据列位置一致。
- 中文示例说明列从 240 → 410px 后，首行由 150px 降到 85px；横向窗口切换仍保留当前布局下已测得的最大行高。
- 10 万行 × 10 万列按需生成示例只渲染约 8–14 行、9 个表头；已检查末行 99999、末中心列 99998、固定列和调整后定位。单测另覆盖 16,777,216 个逻辑列，只为改过的列保存宽度。
- 中英文示例、完整 Code 与 Playground 均实测；检查浅色、暗色和键盘调整。相关组件测试 64 项通过，文档示例/API 测试 4 项通过，组件和文档类型检查通过。
- `typecheck:vitest` 仍有旧测试类型错误（Card、Carousel、VirtualList、utils 等）；本次新增列宽测试未出现类型错误。阶段级性能/内存基准与完整组合验收尚未完成，不因上述窗口计数而勾选公共验收项。
- 首次验收发现既有纵向虚拟列表受浏览器最大布局高度限制：100 万行、估算 64px 的总高度超限，无法正确到达末端。该问题已在下述补充验证中修复。

### 纵向巨量定位补充验证（2026-09-04）

- 生成数据超过安全布局高度时，使用最长 800 万像素的原生轨道，并把可见行映射到实际视口；逻辑行高、行索引、`scrollToOffset` 的逻辑像素契约不变。只渲染窗口，未复制完整数据矩阵。
- 恢复列宽示例为 100 万行 × 10 万列。浏览器已确认末行 999999 和末中心列 99998；末端只挂载 8 行、9 个表头。原生滑块拖到中段后落在约 49 万行，挂载 18 行；轨道始终保持 800 万像素。
- 从末端滚轮上移 80px，同一行的屏幕坐标移动恰好 80px；不会按压缩比例放大滚轮步长。左右固定列保持对齐。
- 末端把中心列 120 → 170px 后重新测量，最后一行仍与滚动视口底部重合。拖动期间不补偿 `scrollTop` 的保护继续通过回归测试。
- 中英文完整 Code 与 Playground 已确认使用百万行源，两个 Playground 均能到达末行 999999。相关表格/虚拟列表测试合计 69 项通过（包含卸载后停止补偿的专项检查），文档/API 测试 4 项、组件类型检查通过。
- 本次验证针对桌面浏览器的生成数据、滚轮、原生滑块和编程定位，不等同于阶段级完整性能、内存、触屏手势及所有功能组合认证；公共验收项仍保持未完成。

## 行选择与文档修订（2026-09-04）

- 行选择统一使用 `v-model:row`，单选值为行对象或 null，多选为数组；同步 TableSelect 内部调用、中英文示例和 API。旧绑定保留迁移兼容，显式 `row` 优先。
- 选中行保留原背景，以贴近行边缘的阴影突出状态；检查浅色、暗色、单选、多选，以及 Code/Playground 双语交互。相关 Table、TableSelect、VirtualList 测试 79 项、文档测试 4 项通过，组件和文档类型检查通过。
- 对外表格文档改为 API 与使用说明，移除实现指令、内部容量讨论和验证口吻。示例可见文案与完整源码同步；后续文档继续面向组件使用者编写。

## 参考

- [VXE Table 官方功能清单](https://github.com/x-extends/vxe-table#功能点)
- [VXE Table 官方文档](https://vxetable.cn/)

此清单描述目标与依赖顺序，不代表已完成，也不承诺一次性交付全部能力。

## 列管理与行高亮验证（2026-09-04）

- `column-manager-config` 默认关闭；`column-state` 控制显隐、完整列序与固定位置，配置列和声明式列共享布局。列管理使用独立的排列模块、状态 composable 和面板组件，未修改消费者列与行对象。
- 面板复用 SPopper、SCheckbox、SSelect、SButton、SVirtualList 和 SFocusTrap。检查了键盘重排、PageUp/PageDown、Escape 焦点恢复、嵌套固定位置选择，以及 Playground 中先关闭面板再关闭主弹窗；浅色、暗色、加载禁用均实测。
- 可选 `storageKey` 只在显式配置时读写 localStorage；非受控状态挂载恢复，受控状态不覆盖父级值。浏览器确认刷新后恢复隐藏列，重置保存空设置；单测覆盖存储失败和切换键，失败不阻断表格操作。
- 调整过的列以稀疏状态记录，面板按需读取窗口。100 万行 × 10 万列下隐藏、重排后仅挂载约 9 个设置项、9 个表头、11–13 行；确认末行 999999、末中心列 99998 和末端列宽调整。单测另覆盖 16,777,216 列的重排、隐藏、固定、列宽和隐藏选择列后的模型保持。
- 普通数据的隐藏列保留排序、筛选和选择语义；重排与固定设置后仍按稳定键调整列宽。组合测试覆盖声明式列、树节点、分页、动态行高和双轴窗口。上述计数不代表完整阶段的内存或性能认证，公共验收项继续保留。
- 根据最新反馈，行选择公开模型改为 `v-model:highlight`，优先级为 highlight、兼容 row、兼容 modelValue。亮色采用主色 HSL 偏转到暖色范围后的浅杏色背景；暗色降低混合强度，保留弱阴影、零边框。单选、复选、旧模型兼容及 TableSelect 内部调用已验证。
- 新列管理、持久化及更新的行选择示例，均同步验证中英文渲染、完整 Code 与 Playground。相关 Table、TableSelect、VirtualList 测试 89 项通过；文档示例/API 测试 4 项、组件与文档类型检查通过。源码 include 规范化仅修改对应示例行号。


## 多级表头验证记录（2026-09-04）

- 配置式 `children` 与声明式 `STableColumn #columns` 共享列树及叶子模型。`#default` 保留单元格作用域，`#header` 可定制组标题；空组不生成数据列。固定位置向下继承，显式 false 可解除继承，不修改消费者列对象。
- 分组标题按实际连续叶子计算跨度，较浅叶子跨行显示。显隐、重排、固定分区和虚拟窗口会正确拆分标题段；叶子继续复用排序、筛选、选择、调整列宽和溢出提示。更新表头行数和数据行的 ARIA 索引。
- 分组树、窗口投影和表头行布局分别位于独立模块。未分组时沿用单行 flex 表头；分组布局按相同固定宽度、最小宽度和剩余空间分配规则对齐。浏览器普通与横向虚拟滚动实测坐标一致，弹性列仅存在不超过 0.02px 的布局舍入差异。
- `virtualSource.headerPath(index)` 只读取可见及预渲染列和固定列，`headerDepth` 固定表头行数。100 万行、10 万列的页面与中英文 Playground 都到达末行 999999、末中心列 99998；当前窗口为 10 行、13–14 个表头单元格。该记录不代表完整内存、性能或所有功能组合认证。
- 新增 6 项测试覆盖嵌套跨度、查询、显隐重排固定、声明式更新与卸载、继承、不变数据、懒树/分页/选择、巨量窗口。Table、TableSelect、VirtualList 及文档测试共 99 项通过；后续统一组标题内容渲染后，相关分组/查询测试另 28 项通过。组件和文档类型检查通过。
- 三个示例的中英文渲染、完整 Code 和 Playground 全部检查；验证虚拟开关、键盘列宽调整、隐藏叶子后跨度收缩、末端定位，以及浅色/暗色 HSL 背景、阴影和零边框。源码 include 已规范化，公共阶段验收清单继续保留未完成。


## 表尾数据行验证记录（2026-09-04）

- `footerData` 提供独立记录，支持单行、多行和稳定 `footerRowKey`。复用当前叶子列、列宽、固定偏移与横向窗口；保留表格底部 `footer` 工具栏插槽，表尾自身不参与正文查询、分页或选择。
- 支持指定列/通用表尾插槽、声明式列 `#footer`、列 footer 函数、命名/内联渲染器 footer、footerFormatter 与原始字段回退；按此顺序解析，正文渲染器不会套到表尾。提供独立 footerAlign、showFooterOverflow 和 footerCellClick。
- 表尾行由独立组件渲染；独立高度 composable 在同一布局的横向窗口间保留最大行高，列布局、宽度、数据或显式 measure 会重测。验证观察器清理、数据更新、稳定行键、空正文、格式化优先级和事件隔离。
- 浏览器发现既有 data-view 的 overflow:hidden 会成为普通固定列的参照滚动容器，右固定列被裁切；改成 overflow:clip，保留圆角裁切并让固定列跟随实际滚动容器。表头、正文和表尾在普通/虚拟横向滚动下坐标一致，键盘将金额列从 160 调至 170px 后三层宽度一致。
- 空正文时使用外层原生横向轨道，并让数据表面提供实际物理宽度；有虚拟正文时仍由正文滚动容器驱动，避免双重平移。切换空正文未增加既有巨量源的初始化行读取次数。
- 100 万行、10 万列示例采用显式汇总公式，不遍历生成行列；页面和双语 Playground 均验证末行 999999、末中心列 99998，以及空正文的末列定位。浏览器当前窗口约 12 个正文行、两行表尾共 10–12 个单元格，空正文轨道宽 100320px。此记录不是全面内存/性能或所有组合认证。
- 三类示例的中英文渲染、完整 Code 和 Playground 全部验证；检查单行/多行切换、传入数据范围变化、声明式与底部插槽并存、明暗主题、弱阴影和零边框。相关 Table、TableSelect、VirtualList 及文档测试共 105 项通过，组件与文档类型检查通过；源码 include 已规范化。阶段级公共验收继续保留未完成。

## 详情展开行验证记录（2026-09-04）

- 配置式与声明式 `type: 'expand'` 列使用同一详情入口。`detailConfig`、`detailExpandedKeys` 与树展开、行高亮分别管理；支持初始展开、条件限制、受控接受/拒绝、公开展开/收起与重新加载方法，不修改业务数据。
- 状态与请求放在独立 composable，数据行与详情由同一个行块承载。`#detail`、`#detail-loading`、`#detail-error` 提供独立内容入口；默认错误界面复用 SButton 重试。加载去重、收起清理、AbortSignal、过期响应忽略、数据数组替换及卸载清理均有测试。
- 详情开关使用原生按钮，验证 Tab、Enter、空格和收起后的焦点恢复。详情区域 ID 在同键的多个表格间保持唯一；普通数据的 ARIA 行索引计入详情与表尾，生成源不会遍历全量数据计算详情索引。
- 虚拟详情强制动态测量，横向窗口间保留当前布局下最大行高；详情缩短或关闭时重新测量。未开启详情不添加行包装 DOM，未展开面板不创建尺寸观察器。浏览器普通虚拟示例移除子表格后，详情高度从约 317px 收缩到 169px，行块同步从 369px 收缩到 221px。
- 100 万行、10 万列的中英文页面/Playground 验证末行 999999、末中心列 99998、左右固定列及可见区域宽度的详情面板；窗口只挂载约 7–10 行、7–9 个表头。末端详情缩短后底部误差小于 1px，收起后底部精确对齐且焦点返回该行按钮。此项不是完整性能或内存认证，公共阶段验收仍未完成。
- 表单与子表格、异步加载、巨量源三类示例的中英文渲染、完整 Code 和 Playground 全部验证；检查暗色窄预览、零边框、弱阴影、错误重试和业务表单值保留。相关 Table、TableSelect、VirtualList 及文档测试共 117 项通过，组件与文档类型检查通过；源码 include 已规范化。

## 单元格与整行编辑验证记录（2026-09-04）

- `editConfig` 显式开启单元格或整行编辑，支持双击、单击、手动触发、F2 和可编辑条件。默认编辑器复用 SInput、SSelect、SDatePicker、SSwitch；数字输入转换为数值。配置式插槽、声明式 `#edit` 和具名编辑渲染器共用管线，展示态与编辑态分别解析。
- 独立编辑 composable 管理活动会话、字段草稿、提交与取消。仅复制变更路径，不修改消费者行数据；`editCommit` 提供 changes 与 updatedRow，由业务接收。验证外部同字段冲突、快照隔离、父组件接受提交后的下一单元格切换，以及卸载后的回调清理。
- Enter 提交、Escape 取消，中文输入法合成中不提交；Ctrl/Meta+Enter 支持选择器等控件。日期和选择弹层先处理自身键盘交互；浏览器验证日期第一次 Escape 只关面板，第二次才取消编辑。编辑结束恢复单元格焦点，虚拟卸载保留草稿后再次定位会恢复输入焦点。
- 百万行、十万列示例仅记录已保存的单元格；双语页面/Playground 均到达末行 999999、末中心列 99998。浏览器窗口约 11–13 行、6–8 个表头，整行编辑单测少于 15 个控件；末端提交后重新进入仍读取已保存值。此记录不等同于完整性能或内存认证。
- 三类新示例的中英文渲染、完整 Code 与 Playground 全部检查，包括内置控件、自定义编辑器和生成源；检查明暗主题、窄预览、零边框及弱阴影。Table、TableSelect、VirtualList 与文档测试合计 131 项通过，组件和文档类型检查通过；源码 include 规范化无额外变更。
- 当前已提供切换、查询/分页、滚动卸载及数据替换的基础策略。独立「编辑生命周期」条目保留未完成，下一步补齐树形/懒加载与外部更新等组合审核；校验、变更追踪和历史操作尚未实现。

## 编辑生命周期验证记录（2026-09-04）

- 独立生命周期 composable 统一处理查询、分页、配置式/声明式列变化和当前行有效性。默认切换单元格提交、查询/分页/列变更取消，均支持显式策略；受控查询拒绝更新时保留草稿，接受更新后才执行策略。原地更新筛选数组也会触发检查。
- 同一数组内替换同键行对象时以 data 原因取消；删除行、收起祖先节点或生成源缩小使活动索引越界时以 view 原因取消，不会提交失效记录。复用既有稳定键索引，不遍历生成源；兄弟节点懒加载导致行位移时保留草稿并更新事件行索引。
- 三种 onScroll 策略均通过实际虚拟编辑器卸载测试。默认 keep 在返回窗口后仍提供草稿；显式 startEdit 恢复焦点。commit/cancel 在卸载后分别发出 scroll 原因，普通分页和查询变化优先使用 page/query 原因。
- 新增双语生命周期示例可切换策略、分页、虚拟滚动、模拟同数组行替换及重新加载。中英文完整 Code 和 Playground 均验证；中文虚拟窗口为 11–12 行，编辑器移出后卸载且保存按钮仍可用，返回后草稿/焦点恢复并正常提交。
- 英文页面确认分页提交，Playground 确认默认分页取消；暗色预览中收起祖先后取消子行编辑，焦点保留在展开按钮。编辑表面使用现有 HSL 弱阴影、零边框，实际窗口下策略控件和操作按钮正常排列。
- Table、TableSelect、VirtualList 与文档回归共 139 项通过，组件和文档类型检查通过；新增 8 项生命周期组合测试涵盖行替换/删除、懒树、受控筛选、分页、声明式列、虚拟卸载及生成源边界。源码 include 已规范化；数据校验、变更历史及阶段级完整容量/性能认证继续保留未完成。

## 数据校验实现进度（2026-09-04，未完成）

- 已新增独立规则模块与可取消的批量状态模块，支持必填、类型、范围/长度、正则及自定义同步/异步规则。规则不修改输入值或消费者 RegExp 状态；错误快照与内部状态分离。
- 批量入口接收惰性目标迭代器，每 100 个目标让出执行时间，支持 AbortSignal、错误数上限及明确的 cancelled/truncated 结果。取消能立即结束等待，即使自定义异步校验没有响应 signal；过期结果不覆盖新的错误状态。
- 编辑状态已提供内部提交校验钩子，保留无校验时的同步路径。校验失败保留草稿；同一草稿的并发提交复用请求；继续输入、取消或外部字段冲突后旧结果不能提交，切换单元格会等待异步校验结果。
- 规则/校验会话新增 11 项测试，提交钩子新增 4 项测试；编辑、生命周期与校验专项合计 37 项通过。当前仍是内部基础，尚未连接 STable 公开校验配置与方法、错误展示、跨页/树形/虚拟定位及双语 Code/Playground；本条目保持未完成，下一步继续这些集成工作。
- 内部基础回归：Table、TableSelect、VirtualList 共 150 项测试通过，组件类型检查通过。尚未宣称校验功能对外完成。

## 数据校验集成验证记录（2026-09-04）

- `validationRules` 与列 `rules` 支持必填、类型、范围、长度、正则和自定义同步/异步校验；声明式列与配置列共用规则。修复内联对象列配置重复注册造成的循环更新，实际规则变化仍会清除旧结果。
- `validationConfig` 显式开启提交前校验，手动 `validateCell`、`validateRow`、`validate` 不依赖编辑开关。单元格模式检查当前字段，整行模式包含未修改的规则字段；失败保留草稿，不发布 editCommit。继续输入、取消、新请求、外部数据替换及卸载使旧请求失效。
- 提供错误快照、定向清理、取消及错误定位方法。全表范围覆盖已提供和已加载的树数据，不请求未加载子节点或远程页；view 范围包含当前页展开、筛选后的行，不只检查虚拟窗口。跨页定位尊重受控分页与展开，隐藏或筛除目标不能定位时返回 false。
- 浏览器确认错误以危险色浅背景、弱阴影和文字显示，无边框。内容和消息独立布局，不挤压输入或树节点缩进；错误 ID 包含表实例、行键、字段和列索引，重复字段列不会产生重复 ID。内置编辑器关联 aria-invalid/aria-describedby，自定义插槽可读取 error/validating。
- 验证跨页异步提交失败后仍保留草稿，定位不会意外取消编辑；键盘 Ctrl+Enter 修正后成功提交。已完成的旧错误在行替换、字段修改后不再显示或参与定位；批次较早字段失效时，不发布过期错误。
- 100 万行 × 10 万列的定点检查可定位第 999999 行、第 99998 列，页面及双语 Playground 均只挂载 13 个窗口行。指定列扫描按需读取，按时间与步数让出执行，AbortSignal 取消后保留先前错误；达到 maxErrors 时明确报告 truncated。上述记录不代表全量扫描吞吐、完整内存或全部组合认证。
- 错误增删会重测虚拟行高；浏览器末端错误行显示时 62px，清除后恢复 42px，没有残留空白。浅色/暗色、普通/虚拟行、固定列与错误消息换行均已检查。
- 三类示例的中英文页面、完整 Code 和 Playground 全部验证，公开配置的类型详情能够展开。Table、TableSelect、VirtualList 回归 160 项通过；随后行高重测与分次错误更新的校验专项 22 项通过。文档示例/API 4 项及组件、文档类型检查通过；源码 include 规范化检查无遗漏。共享 Code/Playground 实现未改动，阶段公共验收项仍未勾选。

## 变更追踪基础验证记录（2026-09-04）

- 新增独立的已接受变更记录模块，按稳定行键合并新增、修改和删除。准备操作不会写入记录；取消或拒绝后不计入已修改数据。新增后编辑仍归为新增，新增后删除抵消，修改回到原值后移除记录。
- 还原计划保留原字段值、属性是否存在及删除位置/相邻行信息；数据所有者接受还原后才清除对应记录。支持单行和全部还原计划；修改后删除仍能恢复修改前的值，未对消费者数据执行原地修改。
- 待接受操作按行键检测竞态，不同行可独立接受。同一行改动后又回到基线也会使旧操作失效；只为仍有待处理操作的行保留临时版本记录。业务确认带版本检查，过期确认不会清除较新的变更；重置和释放会使待处理操作失效。
- 字段路径合并保留缺失容器、显式 undefined、数组长度与空位。生成行只访问声明的改动字段，不枚举行对象；字段快照验证了循环引用、日期和 Map，以及长度十亿但只有一个元素的稀疏数组。这不代表可渲染十亿行或全面容量认证。
- 变更记录与字段还原专项 27 项、连同编辑和生命周期回归共 41 项通过，组件类型检查通过。当前仅完成内部基础，公开增删改/查询/还原 API、编辑与校验联动、普通/懒树/生成源的数据接受流程及双语 Code/Playground 尚未接入；「变更追踪」和「撤销与重做」保持未完成。


## 变更追踪接入验证记录（2026-09-04）

- `changeConfig` 默认关闭。普通数组通过 `v-model:data` 接受不可变提案；新增 `insertRows`、`removeRows`、`updateRow`、`revertChanges`、`getChangeRecords`、`acceptChanges`、`resetChanges` 和 `cancelDataChange`，以及数据提案、已接受变更、记录版本事件。方法以稳定行键定位，插入位置按源同级数组解释，不受排序或分页序号影响。
- 编辑草稿先经过已有校验和数据所有者接受流程，成功后才进入记录并发出 `editCommit`；拒绝保留草稿，取消与卸载中止待接受请求。业务保存后的版本确认与本地应用分开；过期确认不能清除新变更。独立数据 API 不隐式运行编辑校验，文档示例在确认基线前显式校验。
- 普通数据只复制受影响同级数组、行和祖先。树分支还原包含隔着未修改父节点的后代及已删除节点；新增后删除抵消，不会随父分支恢复。懒加载只处理已载入数据，并让提案中的显式子数组覆盖旧懒加载缓存。验证消费者数据不被原地修改、空操作不发数据事件、稳定相邻键保持批量插入和恢复顺序。
- 生成源通过 `indexOf` 和 `apply` 适配，更新读取字段补丁，还原不枚举行对象；删除位置包含相邻稳定键。专项覆盖不允许枚举字段的百万行源、源删除后的索引偏移，以及生成数据的新增/修改/删除组合还原；行读取保持有界，未建立全量行列快照。适配器负责稳定键映射、源行数、只读行版本和写入前检查 AbortSignal。
- 浏览器发现源对象更新会被深度列监听误判为列变化，导致已写入的编辑被取消；源列函数现在按引用变化单独监听，专项验证提交记录不会丢失。只更新源行数据或行数时保留调整后的列宽，更换列定义才重置；异步旧编辑结束也不会清除新会话的应用状态。
- 三组中英文示例（普通数组、懒树、生成源）的页面、完整 Code 和共享 Playground 已验证。检查新增与还原、基线确认、必填校验保留草稿、树后代增删还原、键盘开始/取消、浅色和暗色，以及 API 类型详情。生成源页面和双语 Playground 到达末行 999999、末中心列 99998，修改后记录为 1、还原后为 0，当前窗口挂载 11–14 行。
- Table、TableSelect、VirtualList 完整回归 214 项通过，文档示例/API 测试 4 项通过，组件和文档类型检查通过。源码 include 已规范化，未修改共享 Code/Playground 管线。测试工程的全量类型检查仍有 Card、Carousel 等既有错误；本轮新增测试类型问题另行修正并复核。阶段级完整性能/内存和所有功能组合认证仍未完成，不因窗口计数勾选公共验收项。
