# Playground

<card>

## 交互示例

上方编辑组件源码，下方实时预览会随改动更新。可复制片段，或打开完整 Playground 获得分屏代码 + 预览体验。

<PlaygroundEmbed />

</card>

<card>

## 本地开发

克隆仓库并本地运行 play 应用：

<command>

```bash
pnpm install
pnpm dev
```

</command>

然后访问 hash 路由，如 `#/divider`、`#/tabs` 或 `#/upload` 打开单个示例。

</card>

<card>

## 与文档的对应关系

每个组件页都包含可复制的示例代码。Playground 使用相同组件 API — 从这里选择示例，粘贴到项目中，再按组件页 **API** 区调整属性。

</card>
